Real fixtures generated this session, confirming:
- auto_test.udg: real .udg output (376 lines), generated via `-S` flag
  (not a spec setting). Contains arimamdl: (0 1 1)(0 1 1) and
  AutoOutlier$AO1951.May outlier detection -- the real values static()
  needs to resolve.
- resid_test.rsd: real residuals table, from estimate { save = (rsd) },
  same tab-separated format as d10/d11/etc.
