var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"","title":"API Reference"},{"category":"section","location":"api/#The-idiomatic-entry-point","page":"API Reference","text":"","title":"The idiomatic entry point"},{"category":"section","location":"api/#Spec-generation","page":"API Reference","text":"","title":"Spec generation"},{"category":"section","location":"api/#Subprocess-invocation","page":"API Reference","text":"","title":"Subprocess invocation"},{"category":"section","location":"api/#Output-table-parsing","page":"API Reference","text":"","title":"Output-table parsing"},{"category":"section","location":"api/#Diagnostics-(W.5)","page":"API Reference","text":"Typed accessors over .udg, matching R's seasonal package. Each accepts either a raw Dict (fixture/testable with no binary) or an X13Result directly.","title":"Diagnostics (W.5)"},{"category":"section","location":"api/#StatsAPI-contract","page":"API Reference","text":"X13Result implements StatsAPI.aic/bic/aicc/loglikelihood/ nobs/residuals/coef/coefnames/stderror/dof – use these fully-qualified (StatsAPI.aic(r)), not re-exported under their bare names. StatsAPI.vcov always throws (.udg has no covariance matrix).","title":"StatsAPI contract"},{"category":"section","location":"api/#Seasonal-parity-functions","page":"API Reference","text":"","title":"Seasonal-parity functions"},{"category":"section","location":"api/#Plotting-(W.6)","page":"API Reference","text":"RecipesBase.jl recipes – load a plotting backend (using Plots, a Makie backend, ...) to actually draw. plot(r::X13Result) (R's own plot.seas/Python's .plot() in one recipe) needs no separate import; residplot/monthplot/spectrumplot are each a RecipesBase.@userplot wrapper (NOT a plain series-type recipe – X13Result already has its own bare type recipe for plot(r), which would otherwise silently shadow a same-type series-type recipe; see residplot's own docstring for the full, real-binary/real-backend- confirmed story).\n\n@userplot's own macro expansion also generates residplot!/ monthplot!/spectrumplot! (the \"add to an existing plot\" mutating forms, matching plot!) – each carries the same docstring as its non-! counterpart above, not a separate one:","title":"Plotting (W.6)"},{"category":"section","location":"api/#Binary-artifact-management","page":"API Reference","text":"","title":"Binary artifact management"},{"category":"section","location":"api/#Calendars","page":"API Reference","text":"","title":"Calendars"},{"category":"section","location":"api/#Regressor-generation","page":"API Reference","text":"","title":"Regressor generation"},{"category":"function","location":"api/#SeasonalAdjustment.x13","page":"API Reference","text":"x13(y; index=tsindex(y), start=nothing, maxorder=nothing, maxdiff=nothing,\n       outlier=false, trading=false, kwargs...) -> X13Result\n\nThe idiomatic entry point: builds an X13Spec, runs it via run_x13, and returns a parsed, typed X13Result. maxorder/maxdiff/trading/outlier are named explicitly for discoverability, matching statsmodels.tsa.x13.x13_arima_analysis's own parameter names (Python-style curated ergonomics); anything else – transform, x11_mode, seats, regression_variables, arima_model, regression_user, ... – flows straight through kwargs... into X13Spec unchanged (R-style full passthrough). Together this is the concrete \"genuine superset of R's seas() and Python's x13_arima_analysis()\" signature CLAUDE.md requires, not just a naming exercise – see handoff/w4-api.md section \"The API design requirement.\"\n\ny accepts anything tsvalues does. index (defaulting to tsindex(y), which is nothing for a plain vector and most sliced containers – pass it explicitly the same way other TSAnalytics.jl functions ask for index=) is used to infer start when neither it nor an explicit start=(year, period) is given; falls back to X13Spec's own (1980, 1) default otherwise (only result.dates' labeling is affected either way, not the computed values).\n\nperiod (12 monthly / 4 quarterly, matching X13Spec's own default and validation – see validate!) controls both how start is inferred from index (a quarter number 1-4, derived from the inferred date's month, when period=4) and how output tables/residuals are parsed back (YYYYQQ vs YYYYMM, see parse_table).\n\nThrows ArgumentError immediately if the spec is invalid (see validate!) – before any subprocess is spawned – and throws an ErrorException carrying the binary's own error text if the run itself fails, rather than returning a half-populated result.\n\nsave is deliberately not accepted here (see handoff/w4-api.md) – x13's contract is a fully-populated X13Result, which needs all four tables; use X13Spec/run_x13/parse_output directly for a custom, partial table selection. residuals is rejected the same way and for the same reason – x13() always requests it (see X13Result). For a non-SEATS spec, :d8 (final unmodified SI ratios) is ALSO always saved alongside D10-D13 (W.6) – not one of X13Result's own fields, but present on disk so monthplot's SI-ratio overlay never needs to re-run for an x13()-produced result.\n\n\n\n\n\n","title":"SeasonalAdjustment.x13"},{"category":"type","location":"api/#SeasonalAdjustment.X13Result","page":"API Reference","text":"X13Result\n\nThe result of x13 – seasonally_adjusted/trend/ seasonal_factors/irregular are the parsed D11/D12/D10/D13 (or, for a SEATS spec, S11/S12/S10/S13) tables, matching README.md's own already-published field names exactly. residuals (the D13-adjacent regARIMA residuals, from estimate { save = (rsd) } – a distinct spec block from the decomposition tables, since residuals are a property of the underlying model fit) and udg (the binary's own \"user diagnostics\" key-value dump, requires the -S invocation flag – see parse_udg) are always populated too, matching R's seas() always making this information available rather than requiring an opt-in. spec/ run_result are the actual X13Spec/X13RunResult the run used – x13 returns a typed, introspectable result, not a black box (run_result carries the binary's real stdout/warnings even on success).\n\nDeliberately a plain struct: checked directly against TSAnalytics.jl's actual source (see handoff/w4-api.md) that ClassicalDecomposition and STLDecomposition share no common abstract type to subtype – the open question CLAUDE.md flagged at project scaffolding, resolved here, not re-deferred.\n\n\n\n\n\n","title":"SeasonalAdjustment.X13Result"},{"category":"function","location":"api/#SeasonalAdjustment.static","page":"API Reference","text":"static(result::X13Result) -> X13Spec\n\nResolves an \"automatic\" spec's :auto/automdl/outlier choices into an explicit, reproducible X13Spec – matching R's seasonal package's own static(). Reads exclusively from result.udg, never stdout/HTML. Resolves three things, each confirmed directly against the real binary this session (handoff/w4-addendum-udg-residuals-static.md plus this session's own follow-up verification, which corrected one gap in that handoff – see point 3):\n\nARIMA order: .udg's arimamdl field (e.g. \"(0 1 1)(0 1 1)\") is the model actually used, whether from automdl or already explicit; used verbatim as arima_model, with automdl/maxorder/ maxdiff cleared.\nAutomatically detected outliers: .udg keys of the form AutoOutlier$AO1951.May name each one. Confirmed directly that the key with the AutoOutlier$ prefix stripped (AO1951.May) is itself a valid regression_variables entry – fed back in, it reproduces the identical estimated coefficient as two other equivalent formats (AO1951.5, AO1951.05), so no month-name-to-number conversion is needed. Appended to regression_variables; outlier (automatic detection) is turned off, since these are now pinned explicitly.\nTransform (transform=:auto): the original handoff found no clean .udg field for this and planned to regex the HTML output for \"prefers <strong>log transformation</strong>\" – a real gap, but not the one this session found. Direct testing here found .udg DOES resolve this cleanly, just inconsistently between outcomes: when log wins, a separate aictrans: Log(y) field appears (the top-level transform field itself stays a generic \"Automatic selection\"); when log does NOT win (confirmed with a series containing negative values, where log is mathematically inapplicable), aictrans is simply absent and transform itself directly reads \"No transformation\" instead. Only these two exact, independently confirmed strings are recognized – anything else leaves the spec's existing transform unchanged rather than guessing past what this session actually verified.\n\nAny of the three pieces .udg doesn't resolve (including when the original spec was never automatic in that respect to begin with) is left as-is from result.spec – static() never invents a value it can't trace back to a real .udg field.\n\nNot bit-identical: confirmed directly that re-running the resolved spec reproduces the original automatic run's tables only to within ~1e-6 relative precision, not exactly – the same caveat R's own seasonal::static() documents (RegARIMA's iterative estimation doesn't necessarily converge to bit-identical coefficients when a model is pre-specified vs. discovered through automdl's own search). Compare results with isapprox, not ==.\n\n\n\n\n\n","title":"SeasonalAdjustment.static"},{"category":"type","location":"api/#SeasonalAdjustment.X13Spec","page":"API Reference","text":"X13Spec\n\nA validated, renderable X-13ARIMA-SEATS spec. Combines R's full spec-argument passthrough (regression_variables, arima_model as raw strings) with Python's curated, typed fields (transform, outlier, seasonal_order, ...) – see X13Spec(::AbstractVector) for the actual keyword constructor; this struct itself is just the plain field container.\n\n\n\n\n\n","title":"SeasonalAdjustment.X13Spec"},{"category":"function","location":"api/#SeasonalAdjustment.render","page":"API Reference","text":"render(spec::X13Spec) -> String\n\nRenders spec to .spc text, in the same format already confirmed working against the real binary throughout this project's development (series { ... }, transform { function = ... }, regression { ... }, arima { model = ... }, automdl { }, outlier { }, estimate { save = (rsd) }, x11 { ... } / seats { ... }, each block only emitted if relevant, followed by any spec_args (W.5.4) blocks).\n\nspec_args rendering rules (validated against a block collision at validate! time, not here – see validate!):\n\nKeys are grouped by the part before the first . – that's the block name (\"forecast.maxlead\" -> block forecast, setting maxlead).\nA key with NO dot and an EMPTY value renders as an empty block: spec_args[\"slidingspans\"] = \"\" -> slidingspans { }. A dotless key with a NON-empty value is rejected (ambiguous – there's no defined shape for it) rather than guessed at.\nValues are emitted VERBATIM, no quoting or escaping – a raw passthrough exactly like arima_model's own.\nBlocks and, within a block, settings are emitted in sorted-key order – spec_args is a plain Dict, whose iteration order Julia does not guarantee, so sorting is what makes render's output reproducible across runs.\n\n\n\n\n\n","title":"SeasonalAdjustment.render"},{"category":"function","location":"api/#SeasonalAdjustment.validate!","page":"API Reference","text":"validate!(spec::X13Spec) -> X13Spec\n\nChecks real requirements confirmed directly against the real x13prebuilt binary during this project's development (see handoff/w2-spec.md section 2 and development-sequence.md), throwing ArgumentError with a message that names the actual binary error it's preventing – fast, native, BEFORE any subprocess round-trip:\n\nAn explicit ARIMA model (arima_model/seasonal_order) and automdl/maxorder/maxdiff can't both be given (found while implementing W.4's maxorder/maxdiff passthrough).\nperiod must be 4 (quarterly) or 12 (monthly) – confirmed directly against the real binary, the ONLY two values it accepts for seasonal adjustment at all.\nSeries length >= 3 complete years, i.e. 3 * period observations (36 for monthly, 12 for quarterly – confirmed directly for both: the real binary's own minimum-length error is identical in wording for either period, just scaled).\nregression_user data must cover the series length plus the RegARIMA forecast horizon (1 year = period observations), not just the historical length.\ntransform = :log is required whenever a regression block is combined with x11_mode in (:multiplicative, :logadditive).\nspec_args (W.5.4) can't name a block (transform, x11, automdl, regression, estimate, series, arima, seats, outlier) this struct already renders via a typed field – two sources of truth for one block is a silent-misconfiguration risk worth failing loudly on, not a real binary error to reproduce. A dotless spec_args key (no block.setting shape) must have an empty value – it renders an empty block (\"slidingspans\" => \"\" -> slidingspans { }); a non-empty value on a dotless key has no defined shape and is rejected rather than guessed at.\n\n\n\n\n\n","title":"SeasonalAdjustment.validate!"},{"category":"function","location":"api/#SeasonalAdjustment.write_spec","page":"API Reference","text":"write_spec(spec::X13Spec, path::AbstractString) -> String\n\nRenders and writes spec to path, returning path.\n\n\n\n\n\n","title":"SeasonalAdjustment.write_spec"},{"category":"function","location":"api/#SeasonalAdjustment.generate_specs","page":"API Reference","text":"generate_specs(series_list, options_list; parallel::Bool=true) -> Vector{X13Spec}\n\nBuilds one X13Spec per (series, options) pair. Each spec's construction (including validate!) is independent of every other, so this is genuinely parallelizable for the real batch use case (seasonally adjusting a whole panel of series) – Threads.@threads, guarded by Threads.nthreads() > 1 and a minimum batch size, the same pattern used throughout this project family. parallel=false (or a single thread, or a small batch) falls back to a plain serial loop.\n\n\n\n\n\n","title":"SeasonalAdjustment.generate_specs"},{"category":"type","location":"api/#SeasonalAdjustment.X13RunResult","page":"API Reference","text":"X13RunResult\n\nThe typed result of one x13ashtml invocation – success, stdout (the full captured text; the binary writes ALL of its banner/ warning/error output to stdout, confirmed directly – stderr is always empty), and warnings/errors already extracted from it, instead of leaving the caller to grep raw text themselves (the concrete \"superior to R/Python\" claim for this task: both references return either raw text or leave error inspection entirely manual).\n\ndir/basename locate the run's working directory and the spec's basename (without extension) – x13ashtml writes its output tables (<basename>.<table>) there; use with parse_output or parse_table directly.\n\nsuccess is derived from errors being empty, NOT the process exit code – confirmed directly that x13ashtml exits 0 even when it prints an ERROR: and produces no seasonal adjustment at all.\n\n\n\n\n\n","title":"SeasonalAdjustment.X13RunResult"},{"category":"function","location":"api/#SeasonalAdjustment.run_x13","page":"API Reference","text":"run_x13(spec_path; binary_path=x13_binary_path(), udg::Bool=false) -> X13RunResult\n\nRuns x13ashtml against spec_path (copied into a fresh scratch directory first, see _prepare_run_dir) and returns a typed X13RunResult.\n\nudg=true passes the -S command-line flag, which is what actually produces the .udg diagnostics file – confirmed directly by testing every documented x13ashtml flag individually (handoff/w4-addendum- udg-residuals-static.md): .udg is NOT controlled by anything in the spec file itself (X13Spec/render), only by this flag on the invocation. Parse the result with parse_udg.\n\n\n\n\n\n","title":"SeasonalAdjustment.run_x13"},{"category":"function","location":"api/#SeasonalAdjustment.run_x13_batch","page":"API Reference","text":"run_x13_batch(spec_paths; binary_path=x13_binary_path(), parallel::Bool=true) -> Vector{X13RunResult}\n\nRuns x13ashtml against each of spec_paths. When parallel=true (the default), every command runs inside its own @async task, all started up front and joined with @sync – the already-fast (~10ms) subprocesses genuinely overlap at the OS level, since Julia yields the current task while _run_capture is waiting on its subprocess's pipe, with no Julia-side worker-process layer. This is DELIBERATELY NOT a worker-pool design: handoff/w3-run-parse.md found that pattern (each worker itself spawning a subprocess – Python's ProcessPoolExecutor) made things slower, since the coordination overhead exceeded the ~10ms of actual work being parallelized. The direct async-task approach here was benchmarked for real (not just reasoned structurally) against the real binary – see development-sequence.md's W.3 row for the exact numbers. See _run_capture's own comment for why output capture uses a merged Pipe read directly by the calling task, not an IOBuffer redirect target – that form raced under concurrent use even with each task given its own private buffer.\n\n\n\n\n\n","title":"SeasonalAdjustment.run_x13_batch"},{"category":"function","location":"api/#SeasonalAdjustment.parse_table","page":"API Reference","text":"parse_table(path; period=12) -> Vector{Tuple{Date,Float64}}\n\nParses one x13ashtml output table – a .d10/.d11/.d12/.d13 (X-11) or .s10/.s11/.s12/.s13 (SEATS) file. Both share the exact same internal format (confirmed directly by running a real SEATS spec and inspecting the output, not assumed from documentation): a 2-line header (date\\t<name> then a ------\\t---... separator), then tab-separated <YYYYMM>\\t<value> rows – one parser handles both, dispatching on nothing but the file's own content.\n\nperiod selects how the trailing 2 digits of the 6-char date string are interpreted: 12 (the default) for monthly YYYYMM (01-12); 4 for quarterly YYYYQQ – confirmed directly against a real quarterly fixture that the binary reuses the exact same 6-char width for quarterly output, just with the trailing digits meaning quarter-number (01-04) rather than month-number. A quarter is mapped to the Date of its first month (Q1->month 1, Q2->month 4, Q3->month 7, Q4->month 10). Any other period value, or a trailing-digit value out of range for the given period, throws a clear error rather than silently misparsing.\n\n\n\n\n\n","title":"SeasonalAdjustment.parse_table"},{"category":"function","location":"api/#SeasonalAdjustment.parse_output","page":"API Reference","text":"parse_output(result::X13RunResult, tables; period=12) -> Dict{Symbol,Vector{Tuple{Date,Float64}}}\n\nConvenience wrapper around parse_table for several tables at once, resolving each symbol in tables (e.g. :d10, :s11) against result.dir/result.basename (see run_x13). Deliberately takes the whole X13RunResult rather than a bare basename string (as first sketched in handoff/w3-run-parse.md) – run_x13 runs in a fresh scratch directory per call, not the caller's current working directory, so resolving output paths needs result.dir too; taking the result directly avoids the caller having to track and re-pass it by hand.\n\nperiod (12 monthly / 4 quarterly) is threaded straight through to parse_table for every requested table – callers must pass the same period the underlying spec was run with.\n\n\n\n\n\n","title":"SeasonalAdjustment.parse_output"},{"category":"function","location":"api/#SeasonalAdjustment.parse_udg","page":"API Reference","text":"parse_udg(path) -> Dict{String,String}\n\nParses a .udg file – \"user diagnostics\", key: value pairs one per line, colon-separated. Confirmed directly against a real fixture (handoff/udg_and_residuals/auto_test.udg, 376 lines): every line has exactly one colon, no duplicate keys, so a plain Dict loses nothing. Values are left as raw strings (not parsed into numbers/tuples) – the .udg file mixes plain numbers, coefficient/se/t-stat triples, and free text (e.g. \"Automatic selection\") in ways specific to each key, so a uniform numeric parse would either fail or silently misparse; callers that need a specific key as a number should parse it themselves.\n\nNot produced by any spec-file setting – confirmed by testing every documented x13ashtml flag individually: .udg is only written when the binary is invoked with the -S command-line flag (see run_x13's udg keyword), not by anything in X13Spec/render.\n\n\n\n\n\n","title":"SeasonalAdjustment.parse_udg"},{"category":"function","location":"api/#SeasonalAdjustment.udg","page":"API Reference","text":"udg(r::X13Result) -> Dict{String,String}\nudg(r::X13Result, key::AbstractString) -> Union{String,Nothing}\nudg(r::X13Result, keys::AbstractVector) -> Dict{String,String}\n\nThe raw .udg dict (r.udg – this first form is just a named accessor for the existing field, for symmetry with the other two forms and with R's seasonal::udg(x, stats=NULL)). The second form looks up one key, nothing if absent – never throws (mirrors this file's other W.5 accessors, not R's own fail= argument, which has no Julia analogue needed here). The third form returns the subset of r.udg matching keys, silently omitting any key not present.\n\n\n\n\n\n","title":"SeasonalAdjustment.udg"},{"category":"function","location":"api/#SeasonalAdjustment.transformfunction","page":"API Reference","text":"transformfunction(d) -> Union{Symbol,Nothing}\n\nResolves the transform actually used, normalized to :log/:none. Mirrors R's seasonal::transformfunction().\n\nTwo of these four rules were CONFIRMED DIRECTLY this session (not assumed from the committed fixture, which only has an automatic-selection run) by running explicit transform=:log and transform=:none specs through the real binary with udg=true and inspecting the result:\n\nCondition Returns Confirmed how\naictrans == \"Log(y)\" :log automatic selection, log wins – the committed fixture\ntransform == \"No transformation\" :none automatic selection, log doesn't win (fixture) and an explicit transform=:none spec (confirmed directly this session – same exact string both ways, a real finding: an earlier draft of this accessor's design assumed a separate literal \"None\" string for the explicit case, which does NOT occur in practice)\ntransform == \"Log(y)\" :log an explicit transform=:log spec (confirmed directly this session)\ntransform == \"None\" :none NOT confirmed against any real fixture – kept only as a defensive alias in case another x13ashtml build/version emits it; do not rely on this case being real\nanything else nothing never guess\n\nReused by static rather than duplicated.\n\n\n\n\n\n","title":"SeasonalAdjustment.transformfunction"},{"category":"function","location":"api/#SeasonalAdjustment.arima_model","page":"API Reference","text":"arima_model(d) -> Union{String,Nothing}\n\nThe ARIMA model actually used (e.g. \"(0 1 1)(0 1 1)\"), from arimamdl – whether it came from automdl or was already explicit.\n\n\n\n\n\n","title":"SeasonalAdjustment.arima_model"},{"category":"function","location":"api/#SeasonalAdjustment.mstats","page":"API Reference","text":"mstats(d) -> Union{NamedTuple,Nothing}\n\n(m1=…, m2=…, …, m11=…, q=…, qm2=…, fail=…) – the M1-M11 statistics, overall quality Q, Q-M2, and the f3.fail count. nothing only if f3.q itself is absent – CONFIRMED directly this session (not just inferred) by running a real SEATS spec with -S: a SEATS run's .udg genuinely has no f3.* family at all (M-statistics are an X-11-specific diagnostic, with no SEATS equivalent). Also confirmed directly for a quarterly spec: f3.m01-f3.m11 still has all 11 entries regardless of period – only filters's seasonal_ma scales with period (4 entries for quarterly, 12 for monthly), M-statistics do not.\n\n\n\n\n\n","title":"SeasonalAdjustment.mstats"},{"category":"function","location":"api/#SeasonalAdjustment.qs","page":"API Reference","text":"qs(d; which=:all) -> NamedTuple\n\nQS test for seasonality, before and after adjustment. Mirrors R's seasonal::qs(), which returns all rows as a matrix – which=:all (the default) is the closer analogue here.\n\nwhich Returns .udg key\n:all (original=, sa=, residual=, irregular=), each (statistic=, pvalue=) or nothing –\n:original (statistic=, pvalue=) or nothing qsori\n:sa as above qssadj\n:residual as above qsrsd\n:irregular as above qsirr\n\nDeliberately does NOT expose the qss* (extreme-value-adjusted) and *evadj variants at this level – still reachable via udg(r, \"qssori\") etc.\n\n\n\n\n\n","title":"SeasonalAdjustment.qs"},{"category":"function","location":"api/#SeasonalAdjustment.outliers","page":"API Reference","text":"outliers(d; full=false) -> Vector{NamedTuple}\n\nParses AutoOutlier$<TYPE><YEAR>.<PERIOD> keys (e.g. AutoOutlier$AO1951.May in the committed fixture). Mirrors R's seasonal::outlier(x, full=FALSE).\n\nlabel (e.g. \"AO1951.May\") is the AutoOutlier$ prefix stripped and NOTHING ELSE – confirmed directly (W.4a) that this exact string is itself a valid regression_variables entry the binary re-accepts verbatim, reproducing the identical estimated coefficient; do not convert the month name to a number for label. period, by contrast, IS converted to an integer (5 for \"May\") purely for display/analysis – that field is never fed back into a spec, so the conversion doesn't contradict the label rule above. For a quarterly spec, confirmed directly this session (a real level-shift run, period=4): the trailing token is already a plain quarter-number integer (e.g. \"LS2010.1\", not a month abbreviation) – the existing tryparse(Int, ...) fallback below handles this correctly with no special-casing needed, and period here means \"quarter number\" for a quarterly spec the same way it means \"month number\" for a monthly one (see X13Spec's own start field convention). period is nothing only for a genuinely unrecognized trailing token (neither a 3-letter month abbreviation nor a bare integer) – left unresolved rather than guessed.\n\nfull Element shape\nfalse (default) (label=, type=, year=, period=)\ntrue above plus estimate=, stderror=, tstat=\n\nType symbols: :ao, :ls, :tc, :rp, :so, :tls.\n\n\n\n\n\n","title":"SeasonalAdjustment.outliers"},{"category":"function","location":"api/#SeasonalAdjustment.outlier_counts","page":"API Reference","text":"outlier_counts(d) -> NamedTuple\n\n(ao=, ls=, tc=, rp=, so=, tls=, total=) from outlier.ao/.ls/.tc/ .rp/.so/.tls/.total. Each field is nothing if that key is absent (e.g. outlier=false was passed, so no outlier detection ran at all).\n\n\n\n\n\n","title":"SeasonalAdjustment.outlier_counts"},{"category":"function","location":"api/#SeasonalAdjustment.fivebestmdl","page":"API Reference","text":"fivebestmdl(d) -> Union{Vector{NamedTuple},Nothing}\n\n[(model=, bic=), …], up to 5, in rank order, from automdl.best5.mdl01-05/automdl.best5.bic01-05. nothing when automdl didn't run (no automdl.best5.mdl01 key at all). Stops at the first missing index rather than assuming five are always present.\n\n\n\n\n\n","title":"SeasonalAdjustment.fivebestmdl"},{"category":"function","location":"api/#SeasonalAdjustment.seasonality_tests","page":"API Reference","text":"seasonality_tests(d) -> Union{NamedTuple,Nothing}\n\n(stable_f=, stable_d8_f=, kruskal_wallis=, moving_seasonality=, identifiable=), each a plain (F_or_stat, pvalue) tuple except identifiable::Bool (from f2.idseasonal, \"yes\"/\"no\"). nothing if f2.fsb1 itself is absent. Nothing in R's seasonal exposes these as a function – a genuine addition, not just parity.\n\n\n\n\n\n","title":"SeasonalAdjustment.seasonality_tests"},{"category":"function","location":"api/#SeasonalAdjustment.residual_diagnostics","page":"API Reference","text":"residual_diagnostics(d) -> NamedTuple\n\n(durbin_watson=, skewness=, kurtosis=, ljung_box=, n_ljung_box=, n_sig_acf=, n_sig_pacf=). ljung_box is the full lag-indexed table ([(lag=, statistic=, df=, pvalue=), …], from lbq$NN/lblags), not a single value – the raw .udg data itself is lag-indexed, not scalar.\n\n\n\n\n\n","title":"SeasonalAdjustment.residual_diagnostics"},{"category":"function","location":"api/#SeasonalAdjustment.spectral_peaks","page":"API Reference","text":"spectral_peaks(d) -> NamedTuple\n\n(seasonal=, trading_day=), each a Vector{Symbol} naming which series (:ori/:sa/:rsd/:irr) show a visually significant peak, from peaks.seas/peaks.td. Empty vector when the key is absent or reads \"none\". This is what R's own summary output (and what the Python reference pipeline scraped out of the HTML report as trading_day_peak_warning) reduces to.\n\n\n\n\n\n","title":"SeasonalAdjustment.spectral_peaks"},{"category":"function","location":"api/#SeasonalAdjustment.filters","page":"API Reference","text":"filters(d) -> NamedTuple\n\n(seasonal_ma=, trend_ma=, mode=, sa_mode=, unit_root=) – the moving- average filters and mode X-11 actually chose. seasonal_ma is a Vector{String} with one entry per calendar period – confirmed directly, not assumed, that this genuinely scales with period (12 \"MSR\" entries for a monthly spec, 4 for quarterly, checked against a real run of each); trend_ma an Int (falls back to the raw string if it isn't purely numeric); mode a Symbol (:multiplicative etc., from finmode). Empty for a SEATS spec (confirmed directly: SEATS uses no X-11-style seasonal-MA filter at all, so finaltrendma/seasonalma are simply absent from a SEATS .udg).\n\n\n\n\n\n","title":"SeasonalAdjustment.filters"},{"category":"function","location":"api/#SeasonalAdjustment.nobs_effective","page":"API Reference","text":"nobs_effective(d) -> Union{Int,Nothing}\n\nnefobs – the effective observation count AFTER differencing (131 in the committed fixture, vs. StatsAPI.nobs's 144) – distinct from plain nobs because ARIMA differencing costs observations. Needed by residplot (W.6): regARIMA residuals run to nefobs, not nobs, so the residual series is shorter than r.observed/r.dates by exactly that difference, confirmed directly against the fixture (144-131=13 fewer residual observations than the original series).\n\n\n\n\n\n","title":"SeasonalAdjustment.nobs_effective"},{"category":"function","location":"api/#SeasonalAdjustment.spectrum_peaks","page":"API Reference","text":"spectrum_peaks(d; series=:sa) -> Vector{NamedTuple}\n\n(label=, freq=, significant=) for each of the five seasonal (:s1-:s5) and two trading-day (:t1,:t2) frequencies X-13's own spectral analysis reports, for the given series (:original, :sa, :irregular, or :residual, mapping to the .udg prefixes spcori/ spcsa/spcirr/spcrsd). freq is read from the SHARED top-level s1.freq..t2.freq family (confirmed directly: one frequency grid serves all four spectra, not one per series). significant is true only when the raw value has a trailing \"+\" flag (confirmed format: \"15.2 +\"; \"nopeak\" and a bare value with no flag both read false – X-13's documentation describes a wider +/-/blank vocabulary than this fixture happens to exercise, so a \"-\" or blank flag is treated the same conservative way as no flag at all, not guessed to mean something more specific).\n\nDistinct from spectral_peaks (W.5), which only reports WHICH series has any significant peak at all, not which frequency – this is the finer-grained data spectrumplot (W.6) needs for its frequency markers, explicitly deferred out of W.5's own scope for exactly this.\n\n\n\n\n\n","title":"SeasonalAdjustment.spectrum_peaks"},{"category":"function","location":"api/#StatsAPI.dof","page":"API Reference","text":"StatsAPI.dof(r::X13Result) -> Int\n\nnreg + nmodel (regression coefficients plus ARIMA model parameters) – nreg=3, nmodel=2 -> dof=5 in the committed fixture, confirmed directly. Not independently cross-checked against R's own dof(seas_object) semantics; treat this definition as this package's own, not an assumed R-identical one, until that comparison is actually run (see handoff/w5-diagnostics-api-handoff.md section 7.6).\n\n\n\n\n\n","title":"StatsAPI.dof"},{"category":"function","location":"api/#StatsAPI.vcov","page":"API Reference","text":"StatsAPI.vcov(r::X13Result)\n\nAlways throws. .udg carries each coefficient's own standard error (see StatsAPI.stderror) but no covariance matrix between coefficients – there is nothing to return, so this names that plainly rather than returning a wrong or fabricated answer, the same style durbin_watson_test(method=:exact)/fit_garch(dist=:t)-style \"not implemented for this case\" errors use elsewhere in the TSAnalytics.jl family.\n\n\n\n\n\n","title":"StatsAPI.vcov"},{"category":"function","location":"api/#SeasonalAdjustment.series","page":"API Reference","text":"series(r::X13Result, table::Symbol; reeval=true) -> Vector{Float64}\nseries(r::X13Result, tables::AbstractVector{Symbol}; reeval=true) -> Dict{Symbol,Vector{Float64}}\n\nMirrors R's seasonal::series(x, \"d8\"): if table wasn't included in the original run's spec.save, re-runs with it added and returns the freshly-parsed values, rather than forcing the caller down to X13Spec/run_x13/parse_output and rebuilding the spec by hand. The vector form re-runs ONCE for the union of every missing table requested, not once per table.\n\nreeval Behaviour\ntrue (default) Missing table(s) trigger one automatic re-run with save extended to include them (matches R's own default). An @info note is logged when this happens (the Julia idiom for R's verbose=TRUE, suppressible through the logging system).\nfalse A missing table throws ArgumentError naming it, instead of re-running (matches R's reeval=FALSE).\n\nNot cached: every call that needs a re-run performs a fresh subprocess invocation, even if an identical series() call was already made – X13Result is an immutable struct (deliberately, see its own docstring), so caching the re-run's result on it isn't possible without either a mutable scratch field or a wrapper type, neither of which this first pass adds. A caller looping over several tables should use the vector form (one re-run for all of them) rather than calling the scalar form repeatedly.\n\nTable symbols are validated against the union of known X-11/SEATS/ regARIMA table codes before any subprocess is spawned – an unrecognized symbol throws ArgumentError immediately rather than being passed to the binary to fail on.\n\n\n\n\n\n","title":"SeasonalAdjustment.series"},{"category":"function","location":"api/#SeasonalAdjustment.select_order","page":"API Reference","text":"select_order(y; kwargs...) -> NamedTuple\n\n(order=(p,d,q), seasonal_order=(P,D,Q,period), transform=:log|:none) – matches statsmodels.tsa.x13.x13_arima_select_order. Thin: runs x13 with automdl=true (accepting the same kwargs x13 does), then parses the resolved arimamdl string and transformfunction back out of the result.\n\n\n\n\n\n","title":"SeasonalAdjustment.select_order"},{"category":"function","location":"api/#SeasonalAdjustment.open_output","page":"API Reference","text":"open_output(r::X13Result)\n\nR's out(): writes the HTML output and opens it with the platform handler (xdg-open on Linux, open on macOS, start on Windows). Throws ErrorException if the output file doesn't exist (the binary only writes it on a successful run).\n\n\n\n\n\n","title":"SeasonalAdjustment.open_output"},{"category":"function","location":"api/#SeasonalAdjustment.import_spc","page":"API Reference","text":"import_spc(path) -> X13Spec\n\nR's import.spc(): builds an X13Spec from an existing .spc file. Real migration value for anyone arriving from R/Census tooling with existing spec files, rather than hand-translating every block into keyword arguments.\n\nScope, stated plainly rather than implied: parses the same block { key = value ... } shape this package's own render produces. Known blocks (series, transform, regression, arima, automdl, outlier, estimate, x11, seats) map to X13Spec's typed fields; anything else becomes a spec_args entry (W.5.4) under \"blockname.key\". Not a general X-13 grammar parser – comments and unusual multi-line quoting are not handled.\n\nOne confirmed, real information gap: outlier { types = (ao ls tc) } sets outlier=true but the specific types= list is DROPPED, not preserved – X13Spec has no typed field for outlier types yet (see handoff/seasonaladjustment-w5-w10-pipeline-handoff.md §3), and spec_args can't target outlier since it's already a typed-field block (see validate!). Silently losing this would be worse than documenting it: check the source .spc by hand if outlier.types matters for your use case.\n\n\n\n\n\n","title":"SeasonalAdjustment.import_spc"},{"category":"function","location":"api/#SeasonalAdjustment.residplot","page":"API Reference","text":"residplot(r::X13Result; outliers=true)\n\nMatches R's residplot – regARIMA residuals plotted against r.dates.\n\nA real correction to the plot handoff's own design, not just an implementation detail: the handoff assumed .rsd is shorter than r.dates (nefobs, 131 in the committed fixture, vs. nobs, 144) and built the date axis around tail-aligning a shorter residual window. Checked directly against the real binary this session (re-running BOTH the exact automdl spec that produces the committed auto_test.udg nefobs=131, AND the separately-committed resid_test.rsd fixture's own spec, with estimate{save=(rsd)} added): the real .rsd file has 144 rows in both cases – one residual per original observation, always – nefobs is an internal likelihood/AIC bookkeeping count, NOT the .rsd file's actual length. r.residuals and r.dates are therefore always the same length in practice; the tail-alignment below (r.dates[end-length(r.residuals)+1:end]) is kept anyway as a harmless, purely defensive no-op for a length mismatch that has not actually been observed, rather than assumed away entirely.\n\nAdds a zero reference line (R doesn't draw one; regARIMA residuals are mean-zero by construction and it costs nothing).\n\nThrows ArgumentError if r.residuals is empty.\n\nA real, confirmed architectural finding, not in the handoff at all: residplot/monthplot/spectrumplot can NOT be plain series-type recipes (@recipe function f(::Type{Val{:name}}, r::X13Result; ...)) as the handoff specified, because X13Result ALSO has its own bare type recipe (for plot(r)) – confirmed directly this session (a real Plots.jl/GR smoke test where residplot(r) silently rendered plot(r)'s OWN series instead of the residual data, byte-identical PNG output, caught only by hashing the actual rendered images, not by RecipesBase.apply_recipe-based structural testing, which does not exercise the real plot/residplot/... entry-point functions at all). RecipesPipeline's own _process_userrecipes! dispatches apply_recipe(attrs, args...) on ARGUMENT TYPES alone, before seriestype is ever consulted – so a bare type recipe on X13Result always intercepts every call involving one, regardless of what seriestype a caller requested. The standard, correct fix (used throughout the Plots.jl ecosystem for exactly this \"named plot for a type that already has its own generic recipe\" situation) is RecipesBase.@userplot: a small wrapper struct is the recipe's real dispatch target instead of X13Result directly, sidestepping the conflict by construction. RecipesBase.@userplot ResidPlot also auto-exports residplot/residplot! – see src/SeasonalAdjustment.jl, which does NOT separately export these names itself.\n\n\n\n\n\n","title":"SeasonalAdjustment.residplot"},{"category":"function","location":"api/#SeasonalAdjustment.monthplot","page":"API Reference","text":"monthplot(r::X13Result; choice=:seasonal, siratios=true)\n\nMatches R's monthplot.seas – the seasonal factor (or irregular) laid out by calendar period, one band per month (period=12) or quarter (period=4), each a thick mean-bar plus its own raw cycle-subseries points, with the SI ratios (table D8) overlaid as vertical stems (R's own second layer – the actual diagnostic content of this chart: the stems show the scatter the mean bar smooths through).\n\nKeyword Values Default Meaning\nchoice :seasonal, :irregular :seasonal Which component to lay out\nsiratios Bool true Overlay D8 SI ratios as stems (:seasonal only – ignored, with a warning, under :irregular: SI ratios are seasonal-plus-irregular by construction, so the overlay is meaningless there)\n\nD8 is fetched via series(r, :d8) – already present with no re-run for any x13()-produced result (W.6 added :d8 to x13()'s always-saved set for exactly this); a hand-built X13Spec/run_x13 result triggers series's own automatic (and announced) re-run instead.\n\nImplemented via RecipesBase.@userplot (see residplot's own docstring for exactly why a plain series-type recipe doesn't work here).\n\n\n\n\n\n","title":"SeasonalAdjustment.monthplot"},{"category":"function","location":"api/#SeasonalAdjustment.spectrumplot","page":"API Reference","text":"spectrumplot(r::X13Result; series=:sa)\n\nNot in either reference pipeline – spectrum_peaks (W.6) makes it nearly free, and spectral peaks are how residual seasonality and trading-day effects are actually judged in official statistics. Plots the confirmed real spectrum curve (.sp0/.sp1/.sp2/.spr, fetched via _spectrum_series, re-running once if not already saved) with vertical markers at every seasonal/trading-day frequency .udg itself reports as a visually significant peak (see spectrum_peaks).\n\nKeyword Values Default Meaning\nseries :original, :sa, :irregular, :residual :sa Which spectrum\n\nImplemented via RecipesBase.@userplot (see residplot's own docstring for exactly why a plain series-type recipe doesn't work here).\n\n\n\n\n\n","title":"SeasonalAdjustment.spectrumplot"},{"category":"function","location":"api/#SeasonalAdjustment.residplot!","page":"API Reference","text":"residplot(r::X13Result; outliers=true)\n\nMatches R's residplot – regARIMA residuals plotted against r.dates.\n\nA real correction to the plot handoff's own design, not just an implementation detail: the handoff assumed .rsd is shorter than r.dates (nefobs, 131 in the committed fixture, vs. nobs, 144) and built the date axis around tail-aligning a shorter residual window. Checked directly against the real binary this session (re-running BOTH the exact automdl spec that produces the committed auto_test.udg nefobs=131, AND the separately-committed resid_test.rsd fixture's own spec, with estimate{save=(rsd)} added): the real .rsd file has 144 rows in both cases – one residual per original observation, always – nefobs is an internal likelihood/AIC bookkeeping count, NOT the .rsd file's actual length. r.residuals and r.dates are therefore always the same length in practice; the tail-alignment below (r.dates[end-length(r.residuals)+1:end]) is kept anyway as a harmless, purely defensive no-op for a length mismatch that has not actually been observed, rather than assumed away entirely.\n\nAdds a zero reference line (R doesn't draw one; regARIMA residuals are mean-zero by construction and it costs nothing).\n\nThrows ArgumentError if r.residuals is empty.\n\nA real, confirmed architectural finding, not in the handoff at all: residplot/monthplot/spectrumplot can NOT be plain series-type recipes (@recipe function f(::Type{Val{:name}}, r::X13Result; ...)) as the handoff specified, because X13Result ALSO has its own bare type recipe (for plot(r)) – confirmed directly this session (a real Plots.jl/GR smoke test where residplot(r) silently rendered plot(r)'s OWN series instead of the residual data, byte-identical PNG output, caught only by hashing the actual rendered images, not by RecipesBase.apply_recipe-based structural testing, which does not exercise the real plot/residplot/... entry-point functions at all). RecipesPipeline's own _process_userrecipes! dispatches apply_recipe(attrs, args...) on ARGUMENT TYPES alone, before seriestype is ever consulted – so a bare type recipe on X13Result always intercepts every call involving one, regardless of what seriestype a caller requested. The standard, correct fix (used throughout the Plots.jl ecosystem for exactly this \"named plot for a type that already has its own generic recipe\" situation) is RecipesBase.@userplot: a small wrapper struct is the recipe's real dispatch target instead of X13Result directly, sidestepping the conflict by construction. RecipesBase.@userplot ResidPlot also auto-exports residplot/residplot! – see src/SeasonalAdjustment.jl, which does NOT separately export these names itself.\n\n\n\n\n\nresidplot(r::X13Result; outliers=true)\n\nMatches R's residplot – regARIMA residuals plotted against r.dates.\n\nA real correction to the plot handoff's own design, not just an implementation detail: the handoff assumed .rsd is shorter than r.dates (nefobs, 131 in the committed fixture, vs. nobs, 144) and built the date axis around tail-aligning a shorter residual window. Checked directly against the real binary this session (re-running BOTH the exact automdl spec that produces the committed auto_test.udg nefobs=131, AND the separately-committed resid_test.rsd fixture's own spec, with estimate{save=(rsd)} added): the real .rsd file has 144 rows in both cases – one residual per original observation, always – nefobs is an internal likelihood/AIC bookkeeping count, NOT the .rsd file's actual length. r.residuals and r.dates are therefore always the same length in practice; the tail-alignment below (r.dates[end-length(r.residuals)+1:end]) is kept anyway as a harmless, purely defensive no-op for a length mismatch that has not actually been observed, rather than assumed away entirely.\n\nAdds a zero reference line (R doesn't draw one; regARIMA residuals are mean-zero by construction and it costs nothing).\n\nThrows ArgumentError if r.residuals is empty.\n\nA real, confirmed architectural finding, not in the handoff at all: residplot/monthplot/spectrumplot can NOT be plain series-type recipes (@recipe function f(::Type{Val{:name}}, r::X13Result; ...)) as the handoff specified, because X13Result ALSO has its own bare type recipe (for plot(r)) – confirmed directly this session (a real Plots.jl/GR smoke test where residplot(r) silently rendered plot(r)'s OWN series instead of the residual data, byte-identical PNG output, caught only by hashing the actual rendered images, not by RecipesBase.apply_recipe-based structural testing, which does not exercise the real plot/residplot/... entry-point functions at all). RecipesPipeline's own _process_userrecipes! dispatches apply_recipe(attrs, args...) on ARGUMENT TYPES alone, before seriestype is ever consulted – so a bare type recipe on X13Result always intercepts every call involving one, regardless of what seriestype a caller requested. The standard, correct fix (used throughout the Plots.jl ecosystem for exactly this \"named plot for a type that already has its own generic recipe\" situation) is RecipesBase.@userplot: a small wrapper struct is the recipe's real dispatch target instead of X13Result directly, sidestepping the conflict by construction. RecipesBase.@userplot ResidPlot also auto-exports residplot/residplot! – see src/SeasonalAdjustment.jl, which does NOT separately export these names itself.\n\n\n\n\n\n","title":"SeasonalAdjustment.residplot!"},{"category":"function","location":"api/#SeasonalAdjustment.monthplot!","page":"API Reference","text":"monthplot(r::X13Result; choice=:seasonal, siratios=true)\n\nMatches R's monthplot.seas – the seasonal factor (or irregular) laid out by calendar period, one band per month (period=12) or quarter (period=4), each a thick mean-bar plus its own raw cycle-subseries points, with the SI ratios (table D8) overlaid as vertical stems (R's own second layer – the actual diagnostic content of this chart: the stems show the scatter the mean bar smooths through).\n\nKeyword Values Default Meaning\nchoice :seasonal, :irregular :seasonal Which component to lay out\nsiratios Bool true Overlay D8 SI ratios as stems (:seasonal only – ignored, with a warning, under :irregular: SI ratios are seasonal-plus-irregular by construction, so the overlay is meaningless there)\n\nD8 is fetched via series(r, :d8) – already present with no re-run for any x13()-produced result (W.6 added :d8 to x13()'s always-saved set for exactly this); a hand-built X13Spec/run_x13 result triggers series's own automatic (and announced) re-run instead.\n\nImplemented via RecipesBase.@userplot (see residplot's own docstring for exactly why a plain series-type recipe doesn't work here).\n\n\n\n\n\nmonthplot(r::X13Result; choice=:seasonal, siratios=true)\n\nMatches R's monthplot.seas – the seasonal factor (or irregular) laid out by calendar period, one band per month (period=12) or quarter (period=4), each a thick mean-bar plus its own raw cycle-subseries points, with the SI ratios (table D8) overlaid as vertical stems (R's own second layer – the actual diagnostic content of this chart: the stems show the scatter the mean bar smooths through).\n\nKeyword Values Default Meaning\nchoice :seasonal, :irregular :seasonal Which component to lay out\nsiratios Bool true Overlay D8 SI ratios as stems (:seasonal only – ignored, with a warning, under :irregular: SI ratios are seasonal-plus-irregular by construction, so the overlay is meaningless there)\n\nD8 is fetched via series(r, :d8) – already present with no re-run for any x13()-produced result (W.6 added :d8 to x13()'s always-saved set for exactly this); a hand-built X13Spec/run_x13 result triggers series's own automatic (and announced) re-run instead.\n\nImplemented via RecipesBase.@userplot (see residplot's own docstring for exactly why a plain series-type recipe doesn't work here).\n\n\n\n\n\n","title":"SeasonalAdjustment.monthplot!"},{"category":"function","location":"api/#SeasonalAdjustment.spectrumplot!","page":"API Reference","text":"spectrumplot(r::X13Result; series=:sa)\n\nNot in either reference pipeline – spectrum_peaks (W.6) makes it nearly free, and spectral peaks are how residual seasonality and trading-day effects are actually judged in official statistics. Plots the confirmed real spectrum curve (.sp0/.sp1/.sp2/.spr, fetched via _spectrum_series, re-running once if not already saved) with vertical markers at every seasonal/trading-day frequency .udg itself reports as a visually significant peak (see spectrum_peaks).\n\nKeyword Values Default Meaning\nseries :original, :sa, :irregular, :residual :sa Which spectrum\n\nImplemented via RecipesBase.@userplot (see residplot's own docstring for exactly why a plain series-type recipe doesn't work here).\n\n\n\n\n\nspectrumplot(r::X13Result; series=:sa)\n\nNot in either reference pipeline – spectrum_peaks (W.6) makes it nearly free, and spectral peaks are how residual seasonality and trading-day effects are actually judged in official statistics. Plots the confirmed real spectrum curve (.sp0/.sp1/.sp2/.spr, fetched via _spectrum_series, re-running once if not already saved) with vertical markers at every seasonal/trading-day frequency .udg itself reports as a visually significant peak (see spectrum_peaks).\n\nKeyword Values Default Meaning\nseries :original, :sa, :irregular, :residual :sa Which spectrum\n\nImplemented via RecipesBase.@userplot (see residplot's own docstring for exactly why a plain series-type recipe doesn't work here).\n\n\n\n\n\n","title":"SeasonalAdjustment.spectrumplot!"},{"category":"function","location":"api/#SeasonalAdjustment.x13_binary_path","page":"API Reference","text":"x13_binary_path() -> String\n\nResolves the x13prebuilt artifact for the running platform and returns the actual invokable executable path within it – not just the artifact directory, since each platform's archive has a different internal layout (confirmed directly, see handoff/w1-artifacts.md):\n\nLinux: a bare executable file at the artifact root (x13ashtml), installed via _linux_x13_artifact_dir since it isn't an archive Julia's normal installer can handle.\nWindows: x13ashtml/x13ashtml.exe (a subfolder), installed via _windows_x13_artifact_dir – a real zip archive, but one Julia's normal installer also can't handle (see that function's own docstring for the real bug this surfaced).\nmacOS: x13ashtml/bin/x13ashtml, dynamically linked against three .dylib files in the sibling x13ashtml/lib/ directory – this function asserts that directory and those specific files are present and throws a clear error if not, rather than let a broken extraction fail silently at first actual use. A real tar.gz, which Julia's normal installer genuinely does handle correctly (confirmed directly against a fresh artifact cache, unlike the Windows case above).\n\n\n\n\n\n","title":"SeasonalAdjustment.x13_binary_path"},{"category":"function","location":"api/#SeasonalAdjustment.x13_binary_available","page":"API Reference","text":"x13_binary_available() -> Bool\n\ntrue if the x13prebuilt binary can be resolved AND actually invoked (a trivial no-args run) on this platform, false otherwise (never throws). Exists so W.3/W.4 can give a clear, actionable error – \"the x13prebuilt binary could not be resolved for this platform\" – instead of a cryptic subprocess failure three layers deep.\n\n\n\n\n\n","title":"SeasonalAdjustment.x13_binary_available"},{"category":"type","location":"api/#SeasonalAdjustment.Calendar","page":"API Reference","text":"Calendar\n\nRoot abstract type for every calendar in this package – a QuantLib- style abstraction (isbusinessday/isholiday/isweekend/adjust/ advance/businessdaysbetween/holidaylist), deliberately its own hierarchy rather than a BusinessDays.HolidayCalendar subtype (see the comment above this type for why). TableCalendar is currently the only concrete subtype.\n\n\n\n\n\n","title":"SeasonalAdjustment.Calendar"},{"category":"type","location":"api/#SeasonalAdjustment.TableCalendar","page":"API Reference","text":"TableCalendar <: Calendar\n\nA calendar defined by (1) algorithmically-computable fixed holidays (functions year::Int -> Date), (2) a year-keyed table of moveable holidays with no closed-form date, and (3) a per-calendar set of weekend weekdays (Dates.dayofweek values, Monday=1..Sunday=7).\n\n\n\n\n\n","title":"SeasonalAdjustment.TableCalendar"},{"category":"constant","location":"api/#SeasonalAdjustment.INDIA_NSE","page":"API Reference","text":"INDIA_NSE::TableCalendar\n\nThe India NSE trading calendar. Weekend = Saturday/Sunday (Set([6,7]) in Dates.dayofweek terms). See NSE_MOVEABLE_HOLIDAYS's own docstring-adjacent comment for the honest gaps in the moveable-feast table.\n\n\n\n\n\n","title":"SeasonalAdjustment.INDIA_NSE"},{"category":"function","location":"api/#SeasonalAdjustment.isbusinessday","page":"API Reference","text":"isbusinessday(cal, d) -> Bool\n\ntrue unless d is a weekend (per cal's own weekend set) or a holiday (per cal's own fixed + table holidays).\n\n\n\n\n\n","title":"SeasonalAdjustment.isbusinessday"},{"category":"function","location":"api/#BusinessDays.isholiday","page":"API Reference","text":"isholiday(cal::TableCalendar, d::Date) -> Bool\n\ntrue if d matches one of cal's fixed-date holiday rules or its year-keyed moveable-holiday table. Extends BusinessDays.isholiday.\n\n\n\n\n\n","title":"BusinessDays.isholiday"},{"category":"function","location":"api/#BusinessDays.isweekend","page":"API Reference","text":"isweekend(cal::Calendar, wd::Integer) -> Bool\n\ntrue if weekday wd (a Dates.dayofweek value, Monday=1..Sunday=7) is in cal's own weekend set. Extends BusinessDays.isweekend, whose own single-argument isweekend(dt::Date) method hardcodes Saturday/ Sunday for every calendar – this per-calendar method is exactly the capability Calendar's own docstring explains BusinessDays.jl can't express.\n\n\n\n\n\n","title":"BusinessDays.isweekend"},{"category":"function","location":"api/#SeasonalAdjustment.adjust","page":"API Reference","text":"adjust(cal, d, convention=:following) -> Date\n\nStandard business-day conventions (:following, :preceding, :modified_following, :modified_preceding, :unadjusted), applied to d under cal. Returns d unchanged if it's already a business day (or if convention == :unadjusted).\n\n\n\n\n\n","title":"SeasonalAdjustment.adjust"},{"category":"function","location":"api/#SeasonalAdjustment.advance","page":"API Reference","text":"advance(cal, d, period, convention=:following) -> Date\n\nShifts d by the calendar period (e.g. Dates.Month(1)), then applies adjust with convention to the result.\n\n\n\n\n\n","title":"SeasonalAdjustment.advance"},{"category":"function","location":"api/#SeasonalAdjustment.businessdaysbetween","page":"API Reference","text":"businessdaysbetween(cal, from, to) -> Int\n\nCount of business days in the closed interval [min(from,to), max(from,to)].\n\n\n\n\n\n","title":"SeasonalAdjustment.businessdaysbetween"},{"category":"function","location":"api/#SeasonalAdjustment.holidaylist","page":"API Reference","text":"holidaylist(cal, from, to; include_weekends=false) -> Vector{Date}\n\nHoliday dates in [min(from,to), max(from,to)]. include_weekends additionally includes every weekend date in range (per cal's own weekend set), not just named holidays.\n\nErrors loudly (ArgumentError) if any year spanned by the range has no table_holidays entry in cal – silently falling back to fixed holidays only would look complete while quietly missing most of a real moveable-feast calendar. See handoff/w0-calendars.md section 5.\n\n\n\n\n\n","title":"SeasonalAdjustment.holidaylist"},{"category":"function","location":"api/#SeasonalAdjustment.easter_date","page":"API Reference","text":"easter_date(year) -> Date\n\nEaster Sunday for year, computed by BusinessDays.jl's own easter_date/easter_rata (not re-derived here).\n\n\n\n\n\n","title":"SeasonalAdjustment.easter_date"},{"category":"function","location":"api/#SeasonalAdjustment.trading_day_regressors","page":"API Reference","text":"trading_day_regressors(from, to, cal; freq=:month) -> Matrix{Float64}\n\nFor each period between from and to (freq=:month or freq=:quarter, matching X-13's own period=12/period=4), count actual business days (per cal) on each weekday, returned as a (nperiods, 6) matrix using X-13's own usertype=td contrast convention: column j (Monday=1..Saturday=6) is (# business days on weekday j in that period) - (# business days on Sunday in that period).\n\nKnown gap, freq=:quarter: confirmed directly against the real binary that quarterly's own .rmx export for td has a 7th column (\"Leap Year\") that monthly's does not – this function always returns 6 columns for either freq, so it does not reproduce that extra column. Fine for feeding a user-defined regressor (X-13 doesn't require the extra column there), not a byte-for-byte replica of X-13's own internal quarterly td regressor.\n\n\n\n\n\n","title":"SeasonalAdjustment.trading_day_regressors"},{"category":"function","location":"api/#SeasonalAdjustment.easter_regressor","page":"API Reference","text":"easter_regressor(from, to; window=0, freq=:month) -> Vector{Float64}\n\nThe standard Census/X-13 Easter regressor: for each period (freq=:month or freq=:quarter) between from and to, the fraction of the window-day window immediately before Easter Sunday (of whichever year that period falls in) that overlaps the period. window=0 (the default) produces an all-zero vector – X-13 itself has no universal default w, the user always specifies it explicitly in the .spc file, so no particular value is assumed here either; pass an explicit window (commonly 1, 8, or 15 in practice). freq=:quarter confirmed directly against the real binary to work the same way as monthly (Easter always falls within Q1 or Q2, so at most one quarter per year is affected).\n\n\n\n\n\n","title":"SeasonalAdjustment.easter_regressor"},{"category":"function","location":"api/#SeasonalAdjustment.custom_holiday_regressor","page":"API Reference","text":"custom_holiday_regressor(from, to, cal, holiday_years_present; freq=:month) -> Vector{Float64}\n\nFor each period (freq=:month or freq=:quarter) between from and to, 1.0 if holiday_years_present(year) (a function year::Int -> Union{Date,Nothing}) returns a date that both falls in that period AND is not already a weekend under cal (a holiday that lands on a weekend has no incremental trading-day effect to explain – matches the \"no extra closure\" annotation real NSE holiday listings use for exactly this case), 0.0 otherwise. A year for which holiday_years_present returns nothing contributes 0.0, silently – by design, this function treats \"not applicable this year\" and \"not in the table\" the same way; callers who need to distinguish a genuine gap from a holiday that simply didn't occur should audit coverage with holidaylist first, which errors loudly on an untabulated year.\n\nThis is the exact mechanism verified end-to-end against the real x13prebuilt binary in handoff/verification/diwali_regressor_proof (via regression { user = (...) usertype = (holiday) }) – this function produces the data vector that block needs; W.2 is responsible for writing the block itself.\n\n\n\n\n\n","title":"SeasonalAdjustment.custom_holiday_regressor"},{"category":"section","location":"getting_started/#Getting-Started","page":"Getting Started","text":"","title":"Getting Started"},{"category":"section","location":"getting_started/#Installation","page":"Getting Started","text":"] add https://github.com/MSALabs/SeasonalAdjustment.jl\n\nThe first call that actually needs the x13prebuilt binary (x13, run_x13, or x13_binary_path itself) downloads and installs the right platform-specific artifact automatically (Julia's LazyArtifacts system) — nothing to configure by hand.\n\nnote: About the examples on this page\nEvery example below was actually run, not just written and assumed correct — the pure-Julia ones (calendars, spec construction) are live jldoctest blocks Documenter re-verifies on every docs build; the ones that call the real binary were confirmed directly against it during development (see development-sequence.md's W.0-W.4 rows for the exact values and how each was produced) but are shown as plain code blocks rather than live doctests, since running the actual x13prebuilt binary isn't something every environment that might build these docs can do.","title":"Installation"},{"category":"section","location":"getting_started/#A-first-seasonal-adjustment","page":"Getting Started","text":"x13 accepts anything tsvalues does — a plain Vector, or any other container from the wider TSAnalytics.jl-style ecosystem.\n\nusing SeasonalAdjustment\n\n# The Box-Jenkins airline passengers series (1949-1960), the standard\n# benchmark in this entire field -- this exact call reproduces the\n# real x13prebuilt binary's own D10/D11/D12/D13 output exactly,\n# confirmed directly during this project's development.\nresult = x13(airline_passengers; start=(1949, 1))\n\nresult.seasonally_adjusted   # the D11 table\nresult.trend                 # D12\nresult.seasonal_factors      # D10\nresult.irregular              # D13\nresult.dates                  # Date.(1949-01-01, 1949-02-01, ...)\n\nPrefer SEATS's ARIMA-model-based decomposition instead of X-11's non-parametric filters? Pass seats=true — everything else about the call, and the shape of the returned X13Result, stays the same:\n\nresult = x13(airline_passengers; start=(1949, 1), seats=true, transform=:auto,\n             aictest=[:td, :easter], automdl=true)","title":"A first seasonal adjustment"},{"category":"section","location":"getting_started/#A-genuine-superset-of-R's-and-Python's-own-APIs","page":"Getting Started","text":"R's seas() passes any spec argument through dynamically; Python's x13_arima_analysis() exposes only a curated subset. x13 combines both in one call — Python-style curated options for discoverability, R-style raw passthrough for anything they don't cover:\n\nresult = x13(\n    airline_passengers;\n    start = (1949, 1),\n    seasonal_order = (0, 1, 1, 12),        # Python-style curated ergonomics\n    maxorder = (2, 1), maxdiff = (2, 1),   # matches statsmodels' own parameter names directly\n    trading = true,                         # shorthand for a trading-day regressor\n    regression_variables = [\"easter[1]\"],  # R-style raw passthrough, for anything not curated\n)\n\nA spec that would fail against the real binary is caught immediately, before any subprocess is ever spawned:\n\njulia> using SeasonalAdjustment\n\njulia> x13(collect(1.0:24.0))   # x13prebuilt itself requires >= 36 months (3 complete years)\nERROR: ArgumentError: series has 24 observations, but x13prebuilt requires at least 36 months (3 complete years) of data -- confirmed directly against the real binary's own error (identical wording for both period=12 and period=4, just scaled): \"Series to be modelled and/or seasonally adjusted must have at least 3 complete years of data.\"\n[...]","title":"A genuine superset of R's and Python's own APIs"},{"category":"section","location":"getting_started/#Monthly-and-quarterly-series","page":"Getting Started","text":"X-13ARIMA-SEATS accepts exactly two seasonal periods — period=12 (monthly, the default) and period=4 (quarterly) — confirmed directly against the real binary's own error when anything else is tried (\"Seasonal period must be 4 or 12 if a seasonal adjustment is done\"). start's second element is a subperiod in 1:period (a quarter number 1-4, not a month, when period=4); output tables/residuals are parsed back the same way (YYYYQQ instead of YYYYMM), and the minimum-length requirement scales with period (12 quarters for quarterly, same \"3 complete years\" rule as monthly's 36 months):\n\nresult = x13(quarterly_gdp; start=(1990, 1), period=4, seasonal_order=(0,1,1,4))\n\nOne confirmed, honest gap: trading_day_regressors's quarterly output (freq=:quarter) always has 6 columns, matching monthly's shape — the real binary's own quarterly .rmx export has a 7th \"Leap Year\" column that this function does not reproduce. Not an issue for feeding a user-defined regressor into a spec (X-13 doesn't require the extra column there), just not a byte-for-byte replica of X-13's own internal quarterly td regressor.","title":"Monthly and quarterly series"},{"category":"section","location":"getting_started/#India-aware-calendar-effects","page":"Getting Started","text":"Neither R's nor Python's default setup feeds non-Western holiday effects into RegARIMA out of the box. This package's INDIA_NSE calendar and regressor-generation functions do — entirely in pure Julia, no binary invocation needed to build the regressor data itself:\n\njulia> using SeasonalAdjustment, Dates\n\njulia> isholiday(INDIA_NSE, Date(2025, 10, 21))  # Diwali-Laxmi Pujan 2025\ntrue\n\njulia> isholiday(INDIA_NSE, Date(2025, 1, 26))  # Republic Day\ntrue\n\njulia> isbusinessday(INDIA_NSE, Date(2025, 10, 21))  # a holiday is never also a business day\nfalse\n\njulia> easter_date(2025)\n2025-04-20\n\njulia> diwali_2025_date(year) = year == 2025 ? Date(2025, 10, 21) : nothing;\n\njulia> custom_holiday_regressor(Date(2025, 9, 1), Date(2025, 12, 31), INDIA_NSE, diwali_2025_date)\n4-element Vector{Float64}:\n 0.0\n 1.0\n 0.0\n 0.0\n\nFeed that regressor into a spec via X13Spec's regression_user (or x13's own passthrough of the same keyword) — see the real, end-to-end Diwali-effect proof in development-sequence.md and handoff/verification/diwali_regressor_proof/ for the documented value this exact mechanism reproduces against the real binary (October 1949's seasonal factor shifting from 0.898593816033472 to 0.753973303751993).","title":"India-aware calendar effects"},{"category":"section","location":"getting_started/#Diagnostics:-everything-R's-seasonal-package-exposes","page":"Getting Started","text":"x13() always requests the .udg diagnostics dump (-S), so every value below is already sitting on result with no extra call needed — a typed accessor layer over it (StatsAPI for the model-fit statistics, plain functions for everything else) reads it back out:\n\nresult = x13(airline_passengers; start=(1949, 1), automdl=true, outlier=true,\n             transform=:auto, aictest=[:td, :easter])\n\nusing StatsAPI\nStatsAPI.aic(result)          # 946.662093458382\nStatsAPI.bic(result)          # 963.913277397589\narima_model(result)           # \"(0 1 1)(0 1 1)\"\ntransformfunction(result)     # :log\n\nqs(result).sa                 # (statistic = 0.0, pvalue = 1.0) -- no residual seasonality left\noutliers(result; full=true)   # [(label=\"AO1951.May\", type=:ao, year=1951, period=5,\n                               #   estimate=0.100155824411322, stderror=0.0204386646810968, tstat=4.9003...)]\n\nseries(result, :d8)           # re-runs automatically if :d8 wasn't already saved\n\nAny spec block without a dedicated keyword (forecast, slidingspans, history, ...) is reachable via X13Spec's own spec_args:\n\nresult = x13(airline_passengers; start=(1949, 1),\n             spec_args = Dict(\"forecast.maxlead\" => \"0\"))","title":"Diagnostics: everything R's seasonal package exposes"},{"category":"section","location":"getting_started/#Plotting","page":"Getting Started","text":"Load a plotting backend (Plots.jl below; a Makie backend works too — RecipesBase.jl itself adds zero dependencies of its own) and x13's own result plots directly:\n\nusing SeasonalAdjustment, Plots\n\nresult = x13(airline_passengers; start=(1949, 1))\nplot(result; title=\"Airline Passengers: Original vs Seasonally Adjusted\")\n\n(Image: Original vs seasonally adjusted airline passengers)\n\nmonthplot is the one with the real diagnostic content — the seasonal factor for each calendar month (thick gray line + red mean bar) with the underlying SI ratios overlaid as blue stems, so you can see the scatter the mean bar is actually smoothing through, not just its final value:\n\nmonthplot(result; title=\"Airline Passengers: Seasonal Factors by Month (SI ratios in blue)\")\n\n(Image: Airline passengers seasonal factors by calendar month, with SI-ratio stems)\n\nresidplot (regARIMA residuals) and spectrumplot (spectral peaks — a chart neither R's seasonal nor Python's statsmodels ships at all) round out the set; see the API reference for their full keyword options.","title":"Plotting"},{"category":"section","location":"getting_started/#Design-notes-worth-knowing-before-you-dig-further","page":"Getting Started","text":"The one deliberate exception in the TSAnalytics.jl family. This package wraps the real x13prebuilt binary rather than reimplementing X-11/RegARIMA/SEATS from scratch — see Design principles on the Home page for why, and development-sequence.md for the full policy.\nX13Spec/run_x13/parse_output are the lower-level API behind x13 — reach for them directly if you need a custom, partial table selection (x13() itself always fetches the full D10-D13/ S10-S13 quartet) or want to inspect the rendered .spc text before running it.\nPlatform support: Linux, Windows, and macOS are all resolved via x13_binary_path/x13_binary_available; see development-sequence.md's W.1 row for exactly how each platform's archive layout (a bare file, a zip subfolder, and a bin/+lib/ directory pair, respectively) was confirmed and handled.","title":"Design notes worth knowing before you dig further"},{"category":"section","location":"#SeasonalAdjustment.jl","page":"Home","text":"Official-statistics-grade seasonal adjustment for Julia — X-11, RegARIMA, and SEATS — built on the same trusted, freely-redistributable Census Bureau binary (x13prebuilt) that R's seasonal and Python's statsmodels.tsa.x13 both wrap internally, with India- and other-market-aware calendar effects (Diwali, Holi, and beyond) fed into RegARIMA via x13prebuilt's own user-defined-regressor mechanism — something neither R's nor Python's default setup provides out of the box.\n\nPart of the TSAnalytics.jl project.\n\nStatus: Part 1 (the x13prebuilt wrapper, tasks W.0-W.4) is complete. See development-sequence.md in the repository root for the full staged roadmap, including Part 2 (a from-scratch native Julia engine, not yet started).\n\nNew to the package? Start with Getting Started.\nLooking for a specific function? See the API Reference.","title":"SeasonalAdjustment.jl"},{"category":"section","location":"#Design-principles","page":"Home","text":"Wrap the real binary, don't reimplement it — the one deliberate exception to the TSAnalytics.jl family's usual \"reference, never port\" rule. Rather than reimplementing X-11/RegARIMA/SEATS from scratch, this package's core (Part 1) wraps x13prebuilt directly — the exact same binary R's and Python's own wrapper packages call internally. Both ecosystems already trust this binary for the hardest part of the whole problem (SEATS's spectral factorization especially); wrapping it directly gets correct, production-grade output immediately rather than spending months re-deriving the hardest mathematics in the field before shipping anything. A from-scratch native Julia engine remains a real, planned track (Part 2), deliberately sequenced behind a working wrapper, not abandoned.\nA genuine superset of R's seas() and Python's x13_arima_analysis(), not just a naming exercise. R exposes the entire X-13 spec grammar dynamically via ...; Python exposes only a curated, typed subset. x13's own keyword arguments combine both: R-style raw passthrough (regression_variables, arima_model) for anything the curated fields don't cover, layered with Python-style curated ergonomics (maxorder, maxdiff, trading, outlier, seasonal_order, ... — matching statsmodels.tsa.x13.x13_arima_analysis's own real parameter names) for discoverability.\nValidated before a subprocess round-trip is ever spent. validate! checks every real requirement this project confirmed directly against the actual binary (minimum series length, regressor forecast-horizon coverage, the transform=:log/multiplicative-mode interaction, the ARIMA-vs-automdl conflict) natively, in microseconds, before X13Spec is ever rendered and run — neither R's nor Python's wrapper does this validation at all.\nTyped results, not raw files or bare text. run_x13 returns a real X13RunResult (success, warnings, errors already extracted from the binary's own stdout — confirmed directly that the process exit code alone can't tell success from failure), and x13 returns a typed X13Result, not a directory of output tables the caller has to go find and parse themselves.\nVerified against the real binary directly, not a third-party reimplementation. Since R's and Python's own wrapper packages call the identical binary this package wraps, \"matches R\" and \"matches Python\" both reduce to \"matches x13prebuilt's own real output\" — there is no independent second computation to cross-check against for Part 1. Every stage's committed verification fixtures (handoff/verification/) are real, generated output from actually running the pinned binary, reused as regression fixtures rather than hand-written.\n\nFull detail on all of these — including which findings were confirmed directly against the binary during development, and exactly how each platform (Linux/Windows/macOS) was verified — lives in the repository README and development-sequence.md.","title":"Design principles"}]
}
