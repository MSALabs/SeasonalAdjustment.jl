var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"","title":"API Reference"},{"category":"section","location":"api/#The-idiomatic-entry-point","page":"API Reference","text":"","title":"The idiomatic entry point"},{"category":"section","location":"api/#Spec-generation","page":"API Reference","text":"","title":"Spec generation"},{"category":"section","location":"api/#Subprocess-invocation","page":"API Reference","text":"","title":"Subprocess invocation"},{"category":"section","location":"api/#Output-table-parsing","page":"API Reference","text":"","title":"Output-table parsing"},{"category":"section","location":"api/#Diagnostics","page":"API Reference","text":"Typed accessors over .udg, the binary's own diagnostics dump. Each accepts either a raw Dict (fixture/testable with no binary) or an X13Result directly.","title":"Diagnostics"},{"category":"section","location":"api/#StatsAPI-contract","page":"API Reference","text":"X13Result implements StatsAPI.aic/bic/aicc/loglikelihood/ nobs/residuals/coef/coefnames/stderror/dof/vcov – use these fully-qualified (StatsAPI.aic(r)), not re-exported under their bare names. StatsAPI.vcov reads the real regression/ARIMA coefficient covariance matrix from .rcm/.acm; StatsBase.coeftable (also fully-qualified, StatsBase already exports its own generic) gives Estimate/Std.Error/t-value for every coefficient with no vcov needed for that much.","title":"StatsAPI contract"},{"category":"section","location":"api/#Seasonal-parity-functions","page":"API Reference","text":"","title":"Seasonal-parity functions"},{"category":"section","location":"api/#Forecasts,-missing-values,-components,-model-summary","page":"API Reference","text":"forecast/backcast re-run automatically (same convention as series) whenever the requested table or level isn't already present; components fetches a regression effect's estimated time path (coef/coefnames give the coefficient itself, this gives its month-by-month factor); update re-runs r.spec with overridden kwargs. SeasonalAdjustment.summary is deliberately not exported – Base already exports its own summary (a different, one-line- descriptive-string contract), so using SeasonalAdjustment would collide with it; call it fully-qualified.","title":"Forecasts, missing values, components, model summary"},{"category":"section","location":"api/#Plotting","page":"API Reference","text":"RecipesBase.jl recipes – load a plotting backend (using Plots, a Makie backend, ...) to actually draw. plot(r::X13Result) needs no separate import; residplot/monthplot/spectrumplot are each a RecipesBase.@userplot wrapper (NOT a plain series-type recipe – X13Result already has its own bare type recipe for plot(r), which would otherwise silently shadow a same-type series-type recipe; see residplot's own docstring for the full, real-binary/real-backend- confirmed story).\n\n@userplot's own macro expansion also generates residplot!/ monthplot!/spectrumplot! (the \"add to an existing plot\" mutating forms, matching plot!) – each carries the same docstring as its non-! counterpart above, not a separate one:","title":"Plotting"},{"category":"section","location":"api/#More-recipes","page":"API Reference","text":"seasonalplot is monthplot's transpose (one line per calendar year); forecastplot/componentplot build on the accessors of the same name above; residdiagplot uses the real .acf/.pcf/.ac2 tables rather than recomputing from r.residuals; spanplot is deliberately scoped to the headline .udg summaries slidingspans/revision_history expose, not full per-span time series (see spanplot's own docstring).","title":"More recipes"},{"category":"section","location":"api/#Binary-artifact-management","page":"API Reference","text":"","title":"Binary artifact management"},{"category":"section","location":"api/#Calendars","page":"API Reference","text":"","title":"Calendars"},{"category":"section","location":"api/#Regressor-generation","page":"API Reference","text":"","title":"Regressor generation"},{"category":"section","location":"api/#Bundled-datasets","page":"API Reference","text":"Four real example datasets ship with the package (data/*.csv, ~30 KB total, plain committed CSV rather than Artifacts.toml – see dataset_info(name) for full provenance on each): airline (Box & Jenkins' Series G, this package's own verification baseline), appliance (the X-13 Reference Manual's own worked example, a genuinely different seasonal shape from airline), appliance_q (appliance aggregated to quarters – :derived, not independently published), and iip_india (India's Index of Industrial Production, MOSPI – a real COVID level shift, licensing terms not independently re-verified this session, see dataset_info(\"iip_india\")). dataset(name) returns a plain (date=, value=) NamedTuple – already a Tables.jl column table with no Tables.jl dependency of its own – that x13 accepts directly (x13(dataset(\"airline\")), start inferred automatically).","title":"Bundled datasets"},{"category":"function","location":"api/#SeasonalAdjustment.x13","page":"API Reference","text":"x13(y; index=tsindex(y), start=nothing, maxorder=nothing, maxdiff=nothing,\n       outlier=false, trading=false, kwargs...) -> X13Result\n\nThe idiomatic entry point: builds an X13Spec, runs it via run_x13, and returns a parsed, typed X13Result. maxorder/maxdiff/trading/outlier are named explicitly for discoverability, matching statsmodels.tsa.x13.x13_arima_analysis's own parameter names (Python-style curated ergonomics); anything else – transform, x11_mode, seats, regression_variables, arima_model, regression_user, ... – flows straight through kwargs... into X13Spec unchanged (R-style full passthrough). Together this is the concrete \"genuine superset of R's seas() and Python's x13_arima_analysis()\" signature CLAUDE.md requires, not just a naming exercise.\n\ny accepts anything tsvalues does. index (defaulting to tsindex(y), which is nothing for a plain vector and most sliced containers – pass it explicitly the same way other TSAnalytics.jl functions ask for index=) is used to infer start when neither it nor an explicit start=(year, period) is given; falls back to X13Spec's own (1980, 1) default otherwise (only result.dates' labeling is affected either way, not the computed values).\n\nperiod (12 monthly / 4 quarterly, matching X13Spec's own default and validation – see validate!) controls both how start is inferred from index (a quarter number 1-4, derived from the inferred date's month, when period=4) and how output tables/residuals are parsed back (YYYYQQ vs YYYYMM, see parse_table).\n\nThrows ArgumentError immediately if the spec is invalid (see validate!) – before any subprocess is spawned – and throws an ErrorException carrying the binary's own error text if the run itself fails, rather than returning a half-populated result.\n\nsave is deliberately not accepted here – x13's contract is a fully-populated X13Result, which needs all four tables; use X13Spec/run_x13/parse_output directly for a custom, partial table selection. residuals is rejected the same way and for the same reason – x13() always requests it (see X13Result). For a non-SEATS spec, :d8 (final unmodified SI ratios) is ALSO always saved alongside D10-D13 – not one of X13Result's own fields, but present on disk so monthplot's SI-ratio overlay never needs to re-run for an x13()-produced result.\n\nmissing_action=:x13 needs an explicit transform (:log/:none/:auto) the SAME way any other regression content does (see validate!'s own rule 5) – confirmed directly: X-13 interpolates the -99999 sentinel via its OWN internal AO-style regressor, which is enough to trigger the \"regression present + default multiplicative mode + no explicit transform\" error, even though this package's own X13Spec never renders a regression{} block for it. Not silently defaulted here – pass transform explicitly.\n\n\n\n\n\n","title":"SeasonalAdjustment.x13"},{"category":"type","location":"api/#SeasonalAdjustment.X13Result","page":"API Reference","text":"X13Result\n\nThe result of x13 – seasonally_adjusted/trend/ seasonal_factors/irregular are the parsed D11/D12/D10/D13 (or, for a SEATS spec, S11/S12/S10/S13) tables, matching README.md's own already-published field names exactly. residuals (the D13-adjacent regARIMA residuals, from estimate { save = (rsd) } – a distinct spec block from the decomposition tables, since residuals are a property of the underlying model fit) and udg (the binary's own \"user diagnostics\" key-value dump, requires the -S invocation flag – see parse_udg) are always populated too, matching R's seas() always making this information available rather than requiring an opt-in. spec/ run_result are the actual X13Spec/X13RunResult the run used – x13 returns a typed, introspectable result, not a black box (run_result carries the binary's real stdout/warnings even on success).\n\nDeliberately a plain struct: checked directly against TSAnalytics.jl's actual source that ClassicalDecomposition and STLDecomposition share no common abstract type to subtype – the open question CLAUDE.md flagged at project scaffolding, resolved here, not re-deferred.\n\n\n\n\n\n","title":"SeasonalAdjustment.X13Result"},{"category":"function","location":"api/#SeasonalAdjustment.static","page":"API Reference","text":"static(result::X13Result) -> X13Spec\n\nResolves an \"automatic\" spec's :auto/automdl/outlier choices into an explicit, reproducible X13Spec – matching R's seasonal package's own static(). Reads exclusively from result.udg, never stdout/HTML. Resolves three things, each confirmed directly against the real binary (see point 3 for a real gap found and closed during verification):\n\nARIMA order: .udg's arimamdl field (e.g. \"(0 1 1)(0 1 1)\") is the model actually used, whether from automdl or already explicit; used verbatim as arima_model, with automdl/maxorder/ maxdiff cleared.\nAutomatically detected outliers: .udg keys of the form AutoOutlier$AO1951.May name each one. Confirmed directly that the key with the AutoOutlier$ prefix stripped (AO1951.May) is itself a valid regression_variables entry – fed back in, it reproduces the identical estimated coefficient as two other equivalent formats (AO1951.5, AO1951.05), so no month-name-to-number conversion is needed. Appended to regression_variables; outlier (automatic detection) is turned off, since these are now pinned explicitly.\nTransform (transform=:auto): regexing the HTML output for \"prefers <strong>log transformation</strong>\" was the fallback plan when no clean .udg field seemed available for this. Direct testing found .udg DOES resolve this cleanly, just inconsistently between outcomes: when log wins, a separate aictrans: Log(y) field appears (the top-level transform field itself stays a generic \"Automatic selection\"); when log does NOT win (confirmed with a series containing negative values, where log is mathematically inapplicable), aictrans is simply absent and transform itself directly reads \"No transformation\" instead. Only these two exact, independently confirmed strings are recognized – anything else leaves the spec's existing transform unchanged rather than guessing past what this session actually verified.\n\nAny of the three pieces .udg doesn't resolve (including when the original spec was never automatic in that respect to begin with) is left as-is from result.spec – static() never invents a value it can't trace back to a real .udg field.\n\nNot bit-identical: confirmed directly that re-running the resolved spec reproduces the original automatic run's tables only to within ~1e-6 relative precision, not exactly – the same caveat R's own seasonal::static() documents (RegARIMA's iterative estimation doesn't necessarily converge to bit-identical coefficients when a model is pre-specified vs. discovered through automdl's own search). Compare results with isapprox, not ==.\n\n\n\n\n\n","title":"SeasonalAdjustment.static"},{"category":"type","location":"api/#SeasonalAdjustment.X13Spec","page":"API Reference","text":"X13Spec\n\nA validated, renderable X-13ARIMA-SEATS spec. Combines R's full spec-argument passthrough (regression_variables, arima_model as raw strings) with Python's curated, typed fields (transform, outlier, seasonal_order, ...) – see X13Spec(::AbstractVector) for the actual keyword constructor; this struct itself is just the plain field container.\n\n\n\n\n\n","title":"SeasonalAdjustment.X13Spec"},{"category":"function","location":"api/#SeasonalAdjustment.render","page":"API Reference","text":"render(spec::X13Spec) -> String\n\nRenders spec to .spc text, in the same format already confirmed working against the real binary throughout this project's development (series { ... }, transform { function = ... }, regression { ... }, arima { model = ... }, automdl { }, outlier { }, estimate { save = (rsd) }, x11 { ... } / seats { ... }, each block only emitted if relevant, followed by any spec_args blocks).\n\n** save is routed per-table to the spec block that actually owns it**, not dumped unconditionally into x11{}/seats{}. This was previously a real, confirmed bug: save=[:hol] (the regression-block holiday-factor table) rendered x11 { save = (hol) }, which the real binary rejects, since .hol is a regression{} table, not an x11{} one – a critical naming trap resolved by src/known_tables.jl's generated _TABLE_BLOCK, which this now keys off. Practically: series(r, :hol)'s automatic re-run (and any hand-built X13Spec(y; save=[:hol, :rsd, :fct]) mixing tables from different blocks) now lands each table in the right block – regression{save=(hol)}, estimate{save=(rsd)}, forecast{save=(fct)} – in one render, one run.\n\nPer-block routing rules:\n\nseries/regression/estimate/outlier/x11/seats already have a typed rendering path (_TYPED_SPEC_BLOCKS) – a save table destined for one of these is appended to that block's own save = (...) line (creating the block, e.g. an otherwise-empty regression{} or estimate{}, if nothing else would have rendered it). x11/seats keep their historical default (d10 d11 d12 d13 / s10 s11 s12 s13) whenever save is nothing OR contains no table from that block, so a save request naming only e.g. a regression table doesn't silently turn off the decomposition step itself.\nEvery other block (forecast, check, force, slidingspans, history, identify, x11regression, spectrum) has no typed field at all – a save table destined for one of these is merged into spec_args as a synthetic \"blockname.save\" entry (unioned with any save the caller already put there directly via spec_args) and rendered through the ordinary spec_args path below. composite-only tables have no rendering path at all (this package has no multi-series aggregation support) and are silently not renderable, though still valid entries in _KNOWN_TABLES for cataloguing purposes.\n\nspec_args rendering rules (validated against a block collision at validate! time, not here – see validate!):\n\nKeys are grouped by the part before the first . – that's the block name (\"forecast.maxlead\" -> block forecast, setting maxlead).\nA key with NO dot and an EMPTY value renders as an empty block: spec_args[\"slidingspans\"] = \"\" -> slidingspans { }. A dotless key with a NON-empty value is rejected (ambiguous – there's no defined shape for it) rather than guessed at.\nValues are emitted VERBATIM, no quoting or escaping – a raw passthrough exactly like arima_model's own.\nBlocks and, within a block, settings are emitted in sorted-key order – spec_args is a plain Dict, whose iteration order Julia does not guarantee, so sorting is what makes render's output reproducible across runs.\n\n\n\n\n\n","title":"SeasonalAdjustment.render"},{"category":"function","location":"api/#SeasonalAdjustment.validate!","page":"API Reference","text":"validate!(spec::X13Spec) -> X13Spec\n\nChecks real requirements confirmed directly against the real x13prebuilt binary during this project's development, throwing ArgumentError with a message that names the actual binary error it's preventing – fast, native, BEFORE any subprocess round-trip:\n\nAn explicit ARIMA model (arima_model/seasonal_order) and automdl/maxorder/maxdiff can't both be given (found while implementing maxorder/maxdiff passthrough).\nperiod must be 4 (quarterly) or 12 (monthly) – confirmed directly against the real binary, the ONLY two values it accepts for seasonal adjustment at all.\nSeries length >= 3 complete years, i.e. 3 * period observations (36 for monthly, 12 for quarterly – confirmed directly for both: the real binary's own minimum-length error is identical in wording for either period, just scaled).\nregression_user data must cover the series length plus the RegARIMA forecast horizon (1 year = period observations), not just the historical length.\ntransform = :log is required whenever a regression block is combined with x11_mode in (:multiplicative, :logadditive) – or x11_mode === nothing, since X-13's own implicit default mode IS multiplicative (confirmed directly: a plain trading=true spec with no explicit x11_mode hits the real binary's own log-transform error, the same as setting x11_mode=:multiplicative by hand would). Only :additive/:pseudoadditive are exempt.\nspec_args can't name a block (transform, x11, automdl, regression, estimate, series, arima, seats, outlier) this struct already renders via a typed field – two sources of truth for one block is a silent-misconfiguration risk worth failing loudly on, not a real binary error to reproduce. A dotless spec_args key (no block.setting shape) must have an empty value – it renders an empty block (\"slidingspans\" => \"\" -> slidingspans { }); a non-empty value on a dotless key has no defined shape and is rejected rather than guessed at.\nEvery symbol in save must be a real X-13 save keyword – checked against _KNOWN_TABLES (generated from the full 281-entry catalogue, see tools/generate_known_tables.jl). print-only keywords (none, all, alltables, default, brief – valid for print, invalid for save, confirmed directly, Manual §3.2) get their own, more specific error rather than falling into the generic \"not a recognized table\" message. This closes a real gap: previously, an unrecognized or print-only symbol in save was accepted silently here and either produced a confusing binary-side error (the old code rendered save= verbatim into x11{}/seats{} regardless of which block the table actually belonged to) or, under the newer per-block routing, would have been silently dropped instead – worse. Reject it up front, the same fast-fail convention every other rule here uses.\n\n\n\n\n\n","title":"SeasonalAdjustment.validate!"},{"category":"function","location":"api/#SeasonalAdjustment.write_spec","page":"API Reference","text":"write_spec(spec::X13Spec, path::AbstractString) -> String\n\nRenders and writes spec to path, returning path.\n\n\n\n\n\n","title":"SeasonalAdjustment.write_spec"},{"category":"function","location":"api/#SeasonalAdjustment.generate_specs","page":"API Reference","text":"generate_specs(series_list, options_list; parallel::Bool=true) -> Vector{X13Spec}\n\nBuilds one X13Spec per (series, options) pair. Each spec's construction (including validate!) is independent of every other, so this is genuinely parallelizable for the real batch use case (seasonally adjusting a whole panel of series) – Threads.@threads, guarded by Threads.nthreads() > 1 and a minimum batch size, the same pattern used throughout this project family. parallel=false (or a single thread, or a small batch) falls back to a plain serial loop.\n\n\n\n\n\n","title":"SeasonalAdjustment.generate_specs"},{"category":"type","location":"api/#SeasonalAdjustment.X13RunResult","page":"API Reference","text":"X13RunResult\n\nThe typed result of one x13ashtml invocation – success, stdout (the full captured text; the binary writes ALL of its banner/ warning/error output to stdout, confirmed directly – stderr is always empty), and warnings/errors already extracted from it, instead of leaving the caller to grep raw text themselves (the concrete \"superior to R/Python\" claim for this task: both references return either raw text or leave error inspection entirely manual).\n\ndir/basename locate the run's working directory and the spec's basename (without extension) – x13ashtml writes its output tables (<basename>.<table>) there; use with parse_output or parse_table directly.\n\nsuccess is derived from errors being empty, NOT the process exit code – confirmed directly that x13ashtml exits 0 even when it prints an ERROR: and produces no seasonal adjustment at all.\n\n\n\n\n\n","title":"SeasonalAdjustment.X13RunResult"},{"category":"function","location":"api/#SeasonalAdjustment.run_x13","page":"API Reference","text":"run_x13(spec_path; binary_path=x13_binary_path(), udg::Bool=false) -> X13RunResult\n\nRuns x13ashtml against spec_path (copied into a fresh scratch directory first, see _prepare_run_dir) and returns a typed X13RunResult.\n\nudg=true passes the -S command-line flag, which is what actually produces the .udg diagnostics file – confirmed directly by testing every documented x13ashtml flag individually: .udg is NOT controlled by anything in the spec file itself (X13Spec/render), only by this flag on the invocation. Parse the result with parse_udg.\n\n\n\n\n\n","title":"SeasonalAdjustment.run_x13"},{"category":"function","location":"api/#SeasonalAdjustment.run_x13_batch","page":"API Reference","text":"run_x13_batch(spec_paths; binary_path=x13_binary_path(), parallel::Bool=true) -> Vector{X13RunResult}\n\nRuns x13ashtml against each of spec_paths. When parallel=true (the default), every command runs inside its own @async task, all started up front and joined with @sync – the already-fast (~10ms) subprocesses genuinely overlap at the OS level, since Julia yields the current task while _run_capture is waiting on its subprocess's pipe, with no Julia-side worker-process layer. This is DELIBERATELY NOT a worker-pool design: that pattern (each worker itself spawning a subprocess – Python's ProcessPoolExecutor) made things slower, since the coordination overhead exceeded the ~10ms of actual work being parallelized. The direct async-task approach here was benchmarked for real (not just reasoned structurally) against the real binary. See _run_capture's own comment for why output capture uses a merged Pipe read directly by the calling task, not an IOBuffer redirect target – that form raced under concurrent use even with each task given its own private buffer.\n\n\n\n\n\n","title":"SeasonalAdjustment.run_x13_batch"},{"category":"function","location":"api/#SeasonalAdjustment.parse_table","page":"API Reference","text":"parse_table(path; period=12) -> Vector{Tuple{Date,Float64}}\n\nParses one x13ashtml output table – a .d10/.d11/.d12/.d13 (X-11) or .s10/.s11/.s12/.s13 (SEATS) file. Both share the exact same internal format (confirmed directly by running a real SEATS spec and inspecting the output, not assumed from documentation): a 2-line header (date\\t<name> then a ------\\t---... separator), then tab-separated <YYYYMM>\\t<value> rows – one parser handles both, dispatching on nothing but the file's own content.\n\nperiod selects how the trailing 2 digits of the 6-char date string are interpreted: 12 (the default) for monthly YYYYMM (01-12); 4 for quarterly YYYYQQ – confirmed directly against a real quarterly fixture that the binary reuses the exact same 6-char width for quarterly output, just with the trailing digits meaning quarter-number (01-04) rather than month-number. A quarter is mapped to the Date of its first month (Q1->month 1, Q2->month 4, Q3->month 7, Q4->month 10). Any other period value, or a trailing-digit value out of range for the given period, throws a clear error rather than silently misparsing.\n\n\n\n\n\n","title":"SeasonalAdjustment.parse_table"},{"category":"function","location":"api/#SeasonalAdjustment.parse_output","page":"API Reference","text":"parse_output(result::X13RunResult, tables; period=12) -> Dict{Symbol,Vector{Tuple{Date,Float64}}}\n\nConvenience wrapper around parse_table for several tables at once, resolving each symbol in tables (e.g. :d10, :s11) against result.dir/result.basename (see run_x13). Deliberately takes the whole X13RunResult rather than a bare basename string – run_x13 runs in a fresh scratch directory per call, not the caller's current working directory, so resolving output paths needs result.dir too; taking the result directly avoids the caller having to track and re-pass it by hand.\n\nperiod (12 monthly / 4 quarterly) is threaded straight through to parse_table for every requested table – callers must pass the same period the underlying spec was run with.\n\n\n\n\n\n","title":"SeasonalAdjustment.parse_output"},{"category":"function","location":"api/#SeasonalAdjustment.parse_udg","page":"API Reference","text":"parse_udg(path) -> Dict{String,String}\n\nParses a .udg file – \"user diagnostics\", key: value pairs one per line, colon-separated. Confirmed directly against a real 376-line fixture: every line has exactly one colon, no duplicate keys, so a plain Dict loses nothing. Values are left as raw strings (not parsed into numbers/tuples) – the .udg file mixes plain numbers, coefficient/se/t-stat triples, and free text (e.g. \"Automatic selection\") in ways specific to each key, so a uniform numeric parse would either fail or silently misparse; callers that need a specific key as a number should parse it themselves.\n\nNot produced by any spec-file setting – confirmed by testing every documented x13ashtml flag individually: .udg is only written when the binary is invoked with the -S command-line flag (see run_x13's udg keyword), not by anything in X13Spec/render.\n\n\n\n\n\n","title":"SeasonalAdjustment.parse_udg"},{"category":"function","location":"api/#SeasonalAdjustment.udg","page":"API Reference","text":"udg(r::X13Result) -> Dict{String,String}\nudg(r::X13Result, key::AbstractString) -> Union{String,Nothing}\nudg(r::X13Result, keys::AbstractVector) -> Dict{String,String}\n\nThe raw .udg dict (r.udg – this first form is just a named accessor for the existing field, for symmetry with the other two forms and with R's seasonal::udg(x, stats=NULL)). The second form looks up one key, nothing if absent – never throws (mirrors this file's other diagnostics accessors, not R's own fail= argument, which has no Julia analogue needed here). The third form returns the subset of r.udg matching keys, silently omitting any key not present.\n\n\n\n\n\n","title":"SeasonalAdjustment.udg"},{"category":"function","location":"api/#SeasonalAdjustment.transformfunction","page":"API Reference","text":"transformfunction(d) -> Union{Symbol,Nothing}\n\nResolves the transform actually used, normalized to :log/:none. Mirrors R's seasonal::transformfunction().\n\nTwo of these four rules were CONFIRMED DIRECTLY this session (not assumed from the committed fixture, which only has an automatic-selection run) by running explicit transform=:log and transform=:none specs through the real binary with udg=true and inspecting the result:\n\nCondition Returns Confirmed how\naictrans == \"Log(y)\" :log automatic selection, log wins – the committed fixture\ntransform == \"No transformation\" :none automatic selection, log doesn't win (fixture) and an explicit transform=:none spec (confirmed directly this session – same exact string both ways, a real finding: an earlier draft of this accessor's design assumed a separate literal \"None\" string for the explicit case, which does NOT occur in practice)\ntransform == \"Log(y)\" :log an explicit transform=:log spec (confirmed directly this session)\ntransform == \"None\" :none NOT confirmed against any real fixture – kept only as a defensive alias in case another x13ashtml build/version emits it; do not rely on this case being real\nanything else nothing never guess\n\nReused by static rather than duplicated.\n\n\n\n\n\n","title":"SeasonalAdjustment.transformfunction"},{"category":"function","location":"api/#SeasonalAdjustment.arima_model","page":"API Reference","text":"arima_model(d) -> Union{String,Nothing}\n\nThe ARIMA model actually used (e.g. \"(0 1 1)(0 1 1)\"), from arimamdl – whether it came from automdl or was already explicit.\n\n\n\n\n\n","title":"SeasonalAdjustment.arima_model"},{"category":"function","location":"api/#SeasonalAdjustment.mstats","page":"API Reference","text":"mstats(d) -> Union{NamedTuple,Nothing}\n\n(m1=…, m2=…, …, m11=…, q=…, qm2=…, fail=…) – the M1-M11 statistics, overall quality Q, Q-M2, and the f3.fail count. nothing only if f3.q itself is absent – CONFIRMED directly this session (not just inferred) by running a real SEATS spec with -S: a SEATS run's .udg genuinely has no f3.* family at all (M-statistics are an X-11-specific diagnostic, with no SEATS equivalent). Also confirmed directly for a quarterly spec: f3.m01-f3.m11 still has all 11 entries regardless of period – only filters's seasonal_ma scales with period (4 entries for quarterly, 12 for monthly), M-statistics do not.\n\n\n\n\n\n","title":"SeasonalAdjustment.mstats"},{"category":"function","location":"api/#SeasonalAdjustment.qs","page":"API Reference","text":"qs(d; which=:all) -> NamedTuple\n\nQS test for seasonality, before and after adjustment. Mirrors R's seasonal::qs(), which returns all rows as a matrix – which=:all (the default) is the closer analogue here.\n\nwhich Returns .udg key\n:all (original=, sa=, residual=, irregular=), each (statistic=, pvalue=) or nothing –\n:original (statistic=, pvalue=) or nothing qsori\n:sa as above qssadj\n:residual as above qsrsd\n:irregular as above qsirr\n\nDeliberately does NOT expose the qss* (extreme-value-adjusted) and *evadj variants at this level – still reachable via udg(r, \"qssori\") etc.\n\n\n\n\n\n","title":"SeasonalAdjustment.qs"},{"category":"function","location":"api/#SeasonalAdjustment.outliers","page":"API Reference","text":"outliers(d; full=false) -> Vector{NamedTuple}\n\nParses AutoOutlier$<TYPE><YEAR>.<PERIOD> keys (e.g. AutoOutlier$AO1951.May in the committed fixture). Mirrors R's seasonal::outlier(x, full=FALSE).\n\nlabel (e.g. \"AO1951.May\") is the AutoOutlier$ prefix stripped and NOTHING ELSE – confirmed directly that this exact string is itself a valid regression_variables entry the binary re-accepts verbatim, reproducing the identical estimated coefficient; do not convert the month name to a number for label. period, by contrast, IS converted to an integer (5 for \"May\") purely for display/analysis – that field is never fed back into a spec, so the conversion doesn't contradict the label rule above. For a quarterly spec, confirmed directly this session (a real level-shift run, period=4): the trailing token is already a plain quarter-number integer (e.g. \"LS2010.1\", not a month abbreviation) – the existing tryparse(Int, ...) fallback below handles this correctly with no special-casing needed, and period here means \"quarter number\" for a quarterly spec the same way it means \"month number\" for a monthly one (see X13Spec's own start field convention). period is nothing only for a genuinely unrecognized trailing token (neither a 3-letter month abbreviation nor a bare integer) – left unresolved rather than guessed.\n\nfull Element shape\nfalse (default) (label=, type=, year=, period=)\ntrue above plus estimate=, stderror=, tstat=\n\nType symbols: :ao, :ls, :tc, :rp, :so, :tls.\n\n\n\n\n\n","title":"SeasonalAdjustment.outliers"},{"category":"function","location":"api/#SeasonalAdjustment.outlier_counts","page":"API Reference","text":"outlier_counts(d) -> NamedTuple\n\n(ao=, ls=, tc=, rp=, so=, tls=, total=) from outlier.ao/.ls/.tc/ .rp/.so/.tls/.total. Each field is nothing if that key is absent (e.g. outlier=false was passed, so no outlier detection ran at all).\n\n\n\n\n\n","title":"SeasonalAdjustment.outlier_counts"},{"category":"function","location":"api/#SeasonalAdjustment.fivebestmdl","page":"API Reference","text":"fivebestmdl(d) -> Union{Vector{NamedTuple},Nothing}\n\n[(model=, bic=), …], up to 5, in rank order, from automdl.best5.mdl01-05/automdl.best5.bic01-05. nothing when automdl didn't run (no automdl.best5.mdl01 key at all). Stops at the first missing index rather than assuming five are always present.\n\n\n\n\n\n","title":"SeasonalAdjustment.fivebestmdl"},{"category":"function","location":"api/#SeasonalAdjustment.seasonality_tests","page":"API Reference","text":"seasonality_tests(d) -> Union{NamedTuple,Nothing}\n\n(stable_f=, stable_d8_f=, kruskal_wallis=, moving_seasonality=, identifiable=), each a plain (F_or_stat, pvalue) tuple except identifiable::Bool (from f2.idseasonal, \"yes\"/\"no\"). nothing if f2.fsb1 itself is absent. Nothing in R's seasonal exposes these as a function – a genuine addition, not just parity.\n\n\n\n\n\n","title":"SeasonalAdjustment.seasonality_tests"},{"category":"function","location":"api/#SeasonalAdjustment.residual_diagnostics","page":"API Reference","text":"residual_diagnostics(d) -> NamedTuple\n\n(durbin_watson=, skewness=, kurtosis=, ljung_box=, n_ljung_box=, n_sig_acf=, n_sig_pacf=). ljung_box is the full lag-indexed table ([(lag=, statistic=, df=, pvalue=), …], from lbq$NN/lblags), not a single value – the raw .udg data itself is lag-indexed, not scalar.\n\n\n\n\n\n","title":"SeasonalAdjustment.residual_diagnostics"},{"category":"function","location":"api/#SeasonalAdjustment.spectral_peaks","page":"API Reference","text":"spectral_peaks(d) -> NamedTuple\n\n(seasonal=, trading_day=), each a Vector{Symbol} naming which series (:ori/:sa/:rsd/:irr) show a visually significant peak, from peaks.seas/peaks.td. Empty vector when the key is absent or reads \"none\". This is what R's own summary output (and what the Python reference pipeline scraped out of the HTML report as trading_day_peak_warning) reduces to.\n\n\n\n\n\n","title":"SeasonalAdjustment.spectral_peaks"},{"category":"function","location":"api/#SeasonalAdjustment.filters","page":"API Reference","text":"filters(d) -> NamedTuple\n\n(seasonal_ma=, trend_ma=, mode=, sa_mode=, unit_root=) – the moving- average filters and mode X-11 actually chose. seasonal_ma is a Vector{String} with one entry per calendar period – confirmed directly, not assumed, that this genuinely scales with period (12 \"MSR\" entries for a monthly spec, 4 for quarterly, checked against a real run of each); trend_ma an Int (falls back to the raw string if it isn't purely numeric); mode a Symbol (:multiplicative etc., from finmode). Empty for a SEATS spec (confirmed directly: SEATS uses no X-11-style seasonal-MA filter at all, so finaltrendma/seasonalma are simply absent from a SEATS .udg).\n\n\n\n\n\n","title":"SeasonalAdjustment.filters"},{"category":"function","location":"api/#SeasonalAdjustment.nobs_effective","page":"API Reference","text":"nobs_effective(d) -> Union{Int,Nothing}\n\nnefobs – the effective observation count AFTER differencing (131 in the committed fixture, vs. StatsAPI.nobs's 144) – distinct from plain nobs because ARIMA differencing costs observations. Needed by residplot: regARIMA residuals run to nefobs, not nobs, so the residual series is shorter than r.observed/r.dates by exactly that difference, confirmed directly against the fixture (144-131=13 fewer residual observations than the original series).\n\n\n\n\n\n","title":"SeasonalAdjustment.nobs_effective"},{"category":"function","location":"api/#SeasonalAdjustment.spectrum_peaks","page":"API Reference","text":"spectrum_peaks(d; series=:sa) -> Vector{NamedTuple}\n\n(label=, freq=, significant=) for each of the five seasonal (:s1-:s5) and two trading-day (:t1,:t2) frequencies X-13's own spectral analysis reports, for the given series (:original, :sa, :irregular, or :residual, mapping to the .udg prefixes spcori/ spcsa/spcirr/spcrsd). freq is read from the SHARED top-level s1.freq..t2.freq family (confirmed directly: one frequency grid serves all four spectra, not one per series). significant is true only when the raw value has a trailing \"+\" flag (confirmed format: \"15.2 +\"; \"nopeak\" and a bare value with no flag both read false – X-13's documentation describes a wider +/-/blank vocabulary than this fixture happens to exercise, so a \"-\" or blank flag is treated the same conservative way as no flag at all, not guessed to mean something more specific).\n\nDistinct from spectral_peaks, which only reports WHICH series has any significant peak at all, not which frequency – this is the finer-grained data spectrumplot needs for its frequency markers, deferred out of the plain diagnostics-accessor scope for exactly this.\n\n\n\n\n\n","title":"SeasonalAdjustment.spectrum_peaks"},{"category":"function","location":"api/#StatsAPI.dof","page":"API Reference","text":"StatsAPI.dof(r::X13Result) -> Int\n\nnreg + nmodel (regression coefficients plus ARIMA model parameters) – nreg=3, nmodel=2 -> dof=5 in the committed fixture, confirmed directly. Not independently cross-checked against R's own dof(seas_object) semantics; treat this definition as this package's own, not an assumed R-identical one, until that comparison is actually run.\n\n\n\n\n\n","title":"StatsAPI.dof"},{"category":"function","location":"api/#StatsAPI.vcov","page":"API Reference","text":"StatsAPI.vcov(r::X13Result) -> Matrix{Float64}\n\nSized length(coef(r)) x length(coef(r)), aligned to StatsAPI.coefnames's own order. The regression-coefficient block comes from .rcm, the ARIMA/ARMA-coefficient block from .acm (re-run via save if not already present, same convention as series); cross-covariance between the two families is NaN (genuinely not reported by X-13, not assumed zero). Throws ErrorException if the model has no coefficients at all (nothing to return – the same reasoning that applies to the pure-.udg case).\n\nVerified, not assumed: .rcm/.acm's row order is checked against .udg's own coefficient count before use (sqrt.(diag(V)) should reproduce StatsAPI.stderror – asserted directly in this package's own test suite, not just hoped for).\n\nA real, honest scope limitation, found directly: a trading=true regression reports a 7th, DERIVED coefficient (\"Trading Day$Sun\", Sunday's effect implied by the other six, not independently estimated) that .udg's own coefficient block includes but .rcm's covariance matrix does NOT (6 rows, not 7) – nregderived in StatsAPI.dof's own docstring is exactly this. Rather than guess which coefficient(s) a count mismatch corresponds to, this throws a clear ErrorException naming the row-count mismatch instead of silently misaligning the matrix. Models whose regression coefficients are all independently estimated (holiday/user/easter-style single regressors, or trading-day WITHOUT the derived 7th term) are unaffected – this is specifically a trading-day-regression gap, not a general one.\n\nA second, separate finding: X-13 does not write .rcm AT ALL for exactly ONE regression coefficient (confirmed directly – a run with only easter[1] produces no .rcm file at all, even on success with no error; the same spec with a second, independent regression variable added DOES produce it). Presumably a 1x1 \"covariance matrix\" is considered redundant with stderror alone. vcov still throws its regular ErrorException in that case (via the same \"output table does not exist\" path series already uses), not a silent empty matrix.\n\n\n\n\n\n","title":"StatsAPI.vcov"},{"category":"function","location":"api/#StatsAPI.coeftable","page":"API Reference","text":"StatsBase.coeftable(r::X13Result) -> StatsBase.CoefTable\n\nEstimate/Std.Error/t value for every coefficient StatsAPI.coef reports, straight from .udg's own three-field coefficient lines (no StatsAPI.vcov needed for this much – _coefficient_lines already carries the t-statistic X-13 itself computed). Extends StatsBase.coeftable (used fully-qualified, matching this file's own StatsAPI.aic-style convention – not re-exported under the bare name, since StatsBase already defines and exports one). p-values are deliberately NOT included: computing one needs a t-distribution CDF, which would mean either a new dependency (Distributions.jl, for one column) or leaning on TSAnalytics.jl exposing one, neither confirmed worth it yet – the same open question applies to a possible future Q-Q plot panel. Flagged here rather than silently guessed at.\n\n\n\n\n\n","title":"StatsAPI.coeftable"},{"category":"function","location":"api/#SeasonalAdjustment.series","page":"API Reference","text":"series(r::X13Result, table::Symbol; reeval=true) -> Vector{Float64}\nseries(r::X13Result, tables::AbstractVector{Symbol}; reeval=true) -> Dict{Symbol,Vector{Float64}}\n\nMirrors R's seasonal::series(x, \"d8\"): if table wasn't included in the original run's spec.save, re-runs with it added and returns the freshly-parsed values, rather than forcing the caller down to X13Spec/run_x13/parse_output and rebuilding the spec by hand. The vector form re-runs ONCE for the union of every missing table requested, not once per table.\n\nreeval Behaviour\ntrue (default) Missing table(s) trigger one automatic re-run with save extended to include them (matches R's own default). An @info note is logged when this happens (the Julia idiom for R's verbose=TRUE, suppressible through the logging system).\nfalse A missing table throws ArgumentError naming it, instead of re-running (matches R's reeval=FALSE).\n\nNot cached: every call that needs a re-run performs a fresh subprocess invocation, even if an identical series() call was already made – X13Result is an immutable struct (deliberately, see its own docstring), so caching the re-run's result on it isn't possible without either a mutable scratch field or a wrapper type, neither of which this first pass adds. A caller looping over several tables should use the vector form (one re-run for all of them) rather than calling the scalar form repeatedly.\n\nTable symbols are validated against the union of known X-11/SEATS/ regARIMA table codes before any subprocess is spawned – an unrecognized symbol throws ArgumentError immediately rather than being passed to the binary to fail on.\n\n\n\n\n\n","title":"SeasonalAdjustment.series"},{"category":"function","location":"api/#SeasonalAdjustment.select_order","page":"API Reference","text":"select_order(y; kwargs...) -> NamedTuple\n\n(order=(p,d,q), seasonal_order=(P,D,Q,period), transform=:log|:none) – matches statsmodels.tsa.x13.x13_arima_select_order. Thin: runs x13 with automdl=true (accepting the same kwargs x13 does), then parses the resolved arimamdl string and transformfunction back out of the result.\n\n\n\n\n\n","title":"SeasonalAdjustment.select_order"},{"category":"function","location":"api/#SeasonalAdjustment.open_output","page":"API Reference","text":"open_output(r::X13Result)\n\nR's out(): writes the HTML output and opens it with the platform handler (xdg-open on Linux, open on macOS, start on Windows). Throws ErrorException if the output file doesn't exist (the binary only writes it on a successful run).\n\n\n\n\n\n","title":"SeasonalAdjustment.open_output"},{"category":"function","location":"api/#SeasonalAdjustment.import_spc","page":"API Reference","text":"import_spc(path) -> X13Spec\n\nR's import.spc(): builds an X13Spec from an existing .spc file. Real migration value for anyone arriving from R/Census tooling with existing spec files, rather than hand-translating every block into keyword arguments.\n\nScope, stated plainly rather than implied: parses the same block { key = value ... } shape this package's own render produces. Known blocks (series, transform, regression, arima, automdl, outlier, estimate, x11, seats) map to X13Spec's typed fields; anything else becomes a spec_args entry  under \"blockname.key\". Not a general X-13 grammar parser – comments and unusual multi-line quoting are not handled.\n\nOne confirmed, real information gap: outlier { types = (ao ls tc) } sets outlier=true but the specific types= list is DROPPED, not preserved – X13Spec has no typed field for outlier types yet, and spec_args can't target outlier since it's already a typed-field block (see validate!). Silently losing this would be worse than documenting it: check the source .spc by hand if outlier.types matters for your use case.\n\n\n\n\n\n","title":"SeasonalAdjustment.import_spc"},{"category":"function","location":"api/#SeasonalAdjustment.forecast","page":"API Reference","text":"forecast(r::X13Result; level=0.95) -> (dates=, point=, lower=, upper=)\n\nPoint forecasts plus their level-width prediction interval, extending r.dates forward – .fct (forecast.forecasts) already carries all three on the original scale (confirmed directly), so nothing needs back-transforming here. Re-runs (via X13Spec's forecast.probability) whenever :fct wasn't already saved OR the requested level differs from whatever probability the original run used – changing level genuinely forces a re-run, since the interval width is computed by the binary itself, not derived after the fact.\n\nProgram limit: maxlead (X-13 default 12) is capped at 120 (pfcst, validate!) – pass maxlead to x13/ X13Spec, not here, to control the horizon itself.\n\n\n\n\n\n","title":"SeasonalAdjustment.forecast"},{"category":"function","location":"api/#SeasonalAdjustment.backcast","page":"API Reference","text":"backcast(r::X13Result; level=0.95) -> (dates=, point=, lower=, upper=)\n\nSame as forecast, extending r.dates BACKWARD instead – .bct (forecast.backcasts). The horizon is controlled by spec_args[\"forecast.maxback\"] (no typed field – see X13Spec's own docstring for why only maxlead got one).\n\n\n\n\n\n","title":"SeasonalAdjustment.backcast"},{"category":"function","location":"api/#SeasonalAdjustment.interpolated","page":"API Reference","text":"interpolated(r::X13Result) -> Vector{Float64}\n\nseries.seriesmvadj (.mv) – the original series with missing values (inserted via x13(y; missing_action=:x13)'s -99999 sentinel) replaced by regARIMA's own estimates. Re-runs via series if .mv wasn't already saved.\n\n\n\n\n\n","title":"SeasonalAdjustment.interpolated"},{"category":"function","location":"api/#SeasonalAdjustment.components","page":"API Reference","text":"components(r::X13Result; which=:all) -> NamedTuple or Union{Nothing,Vector{Float64}}\n\nThe estimated TIME PATH of each regression effect (which=:all -> (trading_day=, holiday=, user=, outlier=, ao=, ls=, tc=, so=), each nothing when that effect isn't part of the model; a single which returns just that vector, or nothing). Given the India-calendar layer, which=:holiday/:user are the point of this: coef(r) gives the Diwali coefficient itself, components(r; which=:user) gives its month-by-month factor – closing the loop componentplot draws.\n\nFetched via series's own re-run machinery (_ensure_saved), so results are announced with the same @info convention. which=:all throws ArgumentError if the model has NO regression effects at all (nothing to return); a single which returns nothing instead in that case – see this function's own source comment on _component_precheck for the one real, flagged gap (a handful of component kinds can't be cheaply distinguished from \"not part of the model\" before a subprocess).\n\n\n\n\n\n","title":"SeasonalAdjustment.components"},{"category":"function","location":"api/#SeasonalAdjustment.update","page":"API Reference","text":"update(r::X13Result; kwargs...) -> X13Result\n\nX13Spec(r.spec; kwargs...) then re-runs (via the same _run_spec tail x13 itself uses) – R's seasonal::update.seas. r itself is untouched (X13Result/X13Spec are both immutable).\n\n\n\n\n\n","title":"SeasonalAdjustment.update"},{"category":"function","location":"api/#SeasonalAdjustment.summary","page":"API Reference","text":"SeasonalAdjustment.summary(r::X13Result) -> X13Summary\n\nComposes arima_model/transformfunction/ mstats/qs/outlier_counts/StatsBase.coeftable into one report – no new capability, just a single convenient bundle. Not exported (confirmed directly: Base already has its own summary, a different one-line-descriptive-string contract – using SeasonalAdjustment would collide with it) – call this fully-qualified, SeasonalAdjustment.summary(r).\n\n\n\n\n\n","title":"SeasonalAdjustment.summary"},{"category":"type","location":"api/#SeasonalAdjustment.X13Summary","page":"API Reference","text":"X13Summary\n\nsummary's return type – a compact report (ARIMA model, transform, N/effective N, AIC/BIC, M7 quality statistic, QS on the seasonally adjusted series, outlier count, and a full StatsBase.coeftable) with its own show, matching R's summary.seas in spirit.\n\n\n\n\n\n","title":"SeasonalAdjustment.X13Summary"},{"category":"function","location":"api/#SeasonalAdjustment.slidingspans","page":"API Reference","text":"slidingspans(r::X13Result) -> Union{Nothing,NamedTuple}\n\nnothing if slidingspans{} wasn't requested (udg(r, \"sspans\") != \"yes\"); otherwise (seasonal_max_pct=, sa_max_pct=, trend_max_pct=, raw=) – the headline \"percentage of months flagged unstable\" figures (ssm7's own 4 values: seasonal/SA-percent-change/trend/TD, confirmed directly against .udg) plus raw, every ss*/s2.*/s3.* key verbatim, for anything not promoted to a typed field.\n\n\n\n\n\n","title":"SeasonalAdjustment.slidingspans"},{"category":"function","location":"api/#SeasonalAdjustment.revision_history","page":"API Reference","text":"revision_history(r::X13Result) -> Union{Nothing,NamedTuple}\n\nnothing if history{} wasn't requested (udg(r, \"history\") != \"yes\"); otherwise (sa_estimates=, raw=) – sa_estimates collects every r0N.lag00.aar.* (average absolute revision) value found (the concurrent-vs-most-recent seasonally adjusted revision history), raw every r0*/revspan key verbatim.\n\n\n\n\n\n","title":"SeasonalAdjustment.revision_history"},{"category":"function","location":"api/#SeasonalAdjustment.residplot","page":"API Reference","text":"residplot(r::X13Result; outliers=true)\n\nMatches R's residplot – regARIMA residuals plotted against r.dates.\n\nA real correction, not just an implementation detail: an earlier design assumed .rsd is shorter than r.dates (nefobs, 131 in the committed fixture, vs. nobs, 144) and built the date axis around tail-aligning a shorter residual window. Checked directly against the real binary (re-running BOTH the exact automdl spec that produces the committed auto_test.udg nefobs=131, AND the separately-committed resid_test.rsd fixture's own spec, with estimate{save=(rsd)} added): the real .rsd file has 144 rows in both cases – one residual per original observation, always – nefobs is an internal likelihood/AIC bookkeeping count, NOT the .rsd file's actual length. r.residuals and r.dates are therefore always the same length in practice; the tail-alignment below (r.dates[end-length(r.residuals)+1:end]) is kept anyway as a harmless, purely defensive no-op for a length mismatch that has not actually been observed, rather than assumed away entirely.\n\nAdds a zero reference line (R doesn't draw one; regARIMA residuals are mean-zero by construction and it costs nothing).\n\nThrows ArgumentError if r.residuals is empty.\n\nA real, confirmed architectural finding: residplot/monthplot/spectrumplot can NOT be plain series-type recipes (@recipe function f(::Type{Val{:name}}, r::X13Result; ...)), because X13Result ALSO has its own bare type recipe (for plot(r)) – confirmed directly (a real Plots.jl/GR smoke test where residplot(r) silently rendered plot(r)'s OWN series instead of the residual data, byte-identical PNG output, caught only by hashing the actual rendered images, not by RecipesBase.apply_recipe-based structural testing, which does not exercise the real plot/residplot/... entry-point functions at all). RecipesPipeline's own _process_userrecipes! dispatches apply_recipe(attrs, args...) on ARGUMENT TYPES alone, before seriestype is ever consulted – so a bare type recipe on X13Result always intercepts every call involving one, regardless of what seriestype a caller requested. The standard, correct fix (used throughout the Plots.jl ecosystem for exactly this \"named plot for a type that already has its own generic recipe\" situation) is RecipesBase.@userplot: a small wrapper struct is the recipe's real dispatch target instead of X13Result directly, sidestepping the conflict by construction. RecipesBase.@userplot ResidPlot also auto-exports residplot/residplot! – see src/SeasonalAdjustment.jl, which does NOT separately export these names itself.\n\n\n\n\n\n","title":"SeasonalAdjustment.residplot"},{"category":"function","location":"api/#SeasonalAdjustment.monthplot","page":"API Reference","text":"monthplot(r::X13Result; choice=:seasonal, siratios=true)\n\nMatches R's monthplot.seas – the seasonal factor (or irregular) laid out by calendar period, one band per month (period=12) or quarter (period=4), each a thick mean-bar plus its own raw cycle-subseries points, with the SI ratios (table D8) overlaid as vertical stems (R's own second layer – the actual diagnostic content of this chart: the stems show the scatter the mean bar smooths through).\n\nKeyword Values Default Meaning\nchoice :seasonal, :irregular :seasonal Which component to lay out\nsiratios Bool true Overlay D8 SI ratios as stems (:seasonal only – ignored, with a warning, under :irregular: SI ratios are seasonal-plus-irregular by construction, so the overlay is meaningless there)\n\nD8 is fetched via series(r, :d8) – already present with no re-run for any x13()-produced result (:d8 is part of x13()'s always-saved set for exactly this); a hand-built X13Spec/run_x13 result triggers series's own automatic (and announced) re-run instead.\n\nImplemented via RecipesBase.@userplot (see residplot's own docstring for exactly why a plain series-type recipe doesn't work here).\n\n\n\n\n\n","title":"SeasonalAdjustment.monthplot"},{"category":"function","location":"api/#SeasonalAdjustment.spectrumplot","page":"API Reference","text":"spectrumplot(r::X13Result; series=:sa)\n\nNot in either reference pipeline – spectrum_peaks makes it nearly free, and spectral peaks are how residual seasonality and trading-day effects are actually judged in official statistics. Plots the confirmed real spectrum curve (.sp0/.sp1/.sp2/.spr, fetched via _spectrum_series, re-running once if not already saved) with vertical markers at every seasonal/trading-day frequency .udg itself reports as a visually significant peak (see spectrum_peaks).\n\nKeyword Values Default Meaning\nseries :original, :sa, :irregular, :residual :sa Which spectrum\n\nImplemented via RecipesBase.@userplot (see residplot's own docstring for exactly why a plain series-type recipe doesn't work here).\n\n\n\n\n\n","title":"SeasonalAdjustment.spectrumplot"},{"category":"function","location":"api/#SeasonalAdjustment.residplot!","page":"API Reference","text":"residplot(r::X13Result; outliers=true)\n\nMatches R's residplot – regARIMA residuals plotted against r.dates.\n\nA real correction, not just an implementation detail: an earlier design assumed .rsd is shorter than r.dates (nefobs, 131 in the committed fixture, vs. nobs, 144) and built the date axis around tail-aligning a shorter residual window. Checked directly against the real binary (re-running BOTH the exact automdl spec that produces the committed auto_test.udg nefobs=131, AND the separately-committed resid_test.rsd fixture's own spec, with estimate{save=(rsd)} added): the real .rsd file has 144 rows in both cases – one residual per original observation, always – nefobs is an internal likelihood/AIC bookkeeping count, NOT the .rsd file's actual length. r.residuals and r.dates are therefore always the same length in practice; the tail-alignment below (r.dates[end-length(r.residuals)+1:end]) is kept anyway as a harmless, purely defensive no-op for a length mismatch that has not actually been observed, rather than assumed away entirely.\n\nAdds a zero reference line (R doesn't draw one; regARIMA residuals are mean-zero by construction and it costs nothing).\n\nThrows ArgumentError if r.residuals is empty.\n\nA real, confirmed architectural finding: residplot/monthplot/spectrumplot can NOT be plain series-type recipes (@recipe function f(::Type{Val{:name}}, r::X13Result; ...)), because X13Result ALSO has its own bare type recipe (for plot(r)) – confirmed directly (a real Plots.jl/GR smoke test where residplot(r) silently rendered plot(r)'s OWN series instead of the residual data, byte-identical PNG output, caught only by hashing the actual rendered images, not by RecipesBase.apply_recipe-based structural testing, which does not exercise the real plot/residplot/... entry-point functions at all). RecipesPipeline's own _process_userrecipes! dispatches apply_recipe(attrs, args...) on ARGUMENT TYPES alone, before seriestype is ever consulted – so a bare type recipe on X13Result always intercepts every call involving one, regardless of what seriestype a caller requested. The standard, correct fix (used throughout the Plots.jl ecosystem for exactly this \"named plot for a type that already has its own generic recipe\" situation) is RecipesBase.@userplot: a small wrapper struct is the recipe's real dispatch target instead of X13Result directly, sidestepping the conflict by construction. RecipesBase.@userplot ResidPlot also auto-exports residplot/residplot! – see src/SeasonalAdjustment.jl, which does NOT separately export these names itself.\n\n\n\n\n\nresidplot(r::X13Result; outliers=true)\n\nMatches R's residplot – regARIMA residuals plotted against r.dates.\n\nA real correction, not just an implementation detail: an earlier design assumed .rsd is shorter than r.dates (nefobs, 131 in the committed fixture, vs. nobs, 144) and built the date axis around tail-aligning a shorter residual window. Checked directly against the real binary (re-running BOTH the exact automdl spec that produces the committed auto_test.udg nefobs=131, AND the separately-committed resid_test.rsd fixture's own spec, with estimate{save=(rsd)} added): the real .rsd file has 144 rows in both cases – one residual per original observation, always – nefobs is an internal likelihood/AIC bookkeeping count, NOT the .rsd file's actual length. r.residuals and r.dates are therefore always the same length in practice; the tail-alignment below (r.dates[end-length(r.residuals)+1:end]) is kept anyway as a harmless, purely defensive no-op for a length mismatch that has not actually been observed, rather than assumed away entirely.\n\nAdds a zero reference line (R doesn't draw one; regARIMA residuals are mean-zero by construction and it costs nothing).\n\nThrows ArgumentError if r.residuals is empty.\n\nA real, confirmed architectural finding: residplot/monthplot/spectrumplot can NOT be plain series-type recipes (@recipe function f(::Type{Val{:name}}, r::X13Result; ...)), because X13Result ALSO has its own bare type recipe (for plot(r)) – confirmed directly (a real Plots.jl/GR smoke test where residplot(r) silently rendered plot(r)'s OWN series instead of the residual data, byte-identical PNG output, caught only by hashing the actual rendered images, not by RecipesBase.apply_recipe-based structural testing, which does not exercise the real plot/residplot/... entry-point functions at all). RecipesPipeline's own _process_userrecipes! dispatches apply_recipe(attrs, args...) on ARGUMENT TYPES alone, before seriestype is ever consulted – so a bare type recipe on X13Result always intercepts every call involving one, regardless of what seriestype a caller requested. The standard, correct fix (used throughout the Plots.jl ecosystem for exactly this \"named plot for a type that already has its own generic recipe\" situation) is RecipesBase.@userplot: a small wrapper struct is the recipe's real dispatch target instead of X13Result directly, sidestepping the conflict by construction. RecipesBase.@userplot ResidPlot also auto-exports residplot/residplot! – see src/SeasonalAdjustment.jl, which does NOT separately export these names itself.\n\n\n\n\n\n","title":"SeasonalAdjustment.residplot!"},{"category":"function","location":"api/#SeasonalAdjustment.monthplot!","page":"API Reference","text":"monthplot(r::X13Result; choice=:seasonal, siratios=true)\n\nMatches R's monthplot.seas – the seasonal factor (or irregular) laid out by calendar period, one band per month (period=12) or quarter (period=4), each a thick mean-bar plus its own raw cycle-subseries points, with the SI ratios (table D8) overlaid as vertical stems (R's own second layer – the actual diagnostic content of this chart: the stems show the scatter the mean bar smooths through).\n\nKeyword Values Default Meaning\nchoice :seasonal, :irregular :seasonal Which component to lay out\nsiratios Bool true Overlay D8 SI ratios as stems (:seasonal only – ignored, with a warning, under :irregular: SI ratios are seasonal-plus-irregular by construction, so the overlay is meaningless there)\n\nD8 is fetched via series(r, :d8) – already present with no re-run for any x13()-produced result (:d8 is part of x13()'s always-saved set for exactly this); a hand-built X13Spec/run_x13 result triggers series's own automatic (and announced) re-run instead.\n\nImplemented via RecipesBase.@userplot (see residplot's own docstring for exactly why a plain series-type recipe doesn't work here).\n\n\n\n\n\nmonthplot(r::X13Result; choice=:seasonal, siratios=true)\n\nMatches R's monthplot.seas – the seasonal factor (or irregular) laid out by calendar period, one band per month (period=12) or quarter (period=4), each a thick mean-bar plus its own raw cycle-subseries points, with the SI ratios (table D8) overlaid as vertical stems (R's own second layer – the actual diagnostic content of this chart: the stems show the scatter the mean bar smooths through).\n\nKeyword Values Default Meaning\nchoice :seasonal, :irregular :seasonal Which component to lay out\nsiratios Bool true Overlay D8 SI ratios as stems (:seasonal only – ignored, with a warning, under :irregular: SI ratios are seasonal-plus-irregular by construction, so the overlay is meaningless there)\n\nD8 is fetched via series(r, :d8) – already present with no re-run for any x13()-produced result (:d8 is part of x13()'s always-saved set for exactly this); a hand-built X13Spec/run_x13 result triggers series's own automatic (and announced) re-run instead.\n\nImplemented via RecipesBase.@userplot (see residplot's own docstring for exactly why a plain series-type recipe doesn't work here).\n\n\n\n\n\n","title":"SeasonalAdjustment.monthplot!"},{"category":"function","location":"api/#SeasonalAdjustment.spectrumplot!","page":"API Reference","text":"spectrumplot(r::X13Result; series=:sa)\n\nNot in either reference pipeline – spectrum_peaks makes it nearly free, and spectral peaks are how residual seasonality and trading-day effects are actually judged in official statistics. Plots the confirmed real spectrum curve (.sp0/.sp1/.sp2/.spr, fetched via _spectrum_series, re-running once if not already saved) with vertical markers at every seasonal/trading-day frequency .udg itself reports as a visually significant peak (see spectrum_peaks).\n\nKeyword Values Default Meaning\nseries :original, :sa, :irregular, :residual :sa Which spectrum\n\nImplemented via RecipesBase.@userplot (see residplot's own docstring for exactly why a plain series-type recipe doesn't work here).\n\n\n\n\n\nspectrumplot(r::X13Result; series=:sa)\n\nNot in either reference pipeline – spectrum_peaks makes it nearly free, and spectral peaks are how residual seasonality and trading-day effects are actually judged in official statistics. Plots the confirmed real spectrum curve (.sp0/.sp1/.sp2/.spr, fetched via _spectrum_series, re-running once if not already saved) with vertical markers at every seasonal/trading-day frequency .udg itself reports as a visually significant peak (see spectrum_peaks).\n\nKeyword Values Default Meaning\nseries :original, :sa, :irregular, :residual :sa Which spectrum\n\nImplemented via RecipesBase.@userplot (see residplot's own docstring for exactly why a plain series-type recipe doesn't work here).\n\n\n\n\n\n","title":"SeasonalAdjustment.spectrumplot!"},{"category":"function","location":"api/#SeasonalAdjustment.seasonalplot","page":"API Reference","text":"seasonalplot(r::X13Result; series=:seasonal, polar=false, highlight=nothing)\n\n** The genuinely missing chart** – monthplot's transpose: calendar period on the x-axis, one line per YEAR, all overlaid, so an evolving seasonal pattern (the shape of the year drifting) is visible at a glance, the thing a subseries plot like monthplot can't show directly. Matches R's forecast::ggseasonplot/feasts::gg_season.\n\nKeyword Values Default Meaning\nseries :observed, :seasonal, :sa, :trend, :irregular :seasonal Which component to lay out\npolar Bool false Wrap onto a polar axis (gg_season's polar=TRUE) – backend support for this varies (confirmed Plots.jl-specific, not independently checked against every backend); decide per-backend rather than assuming\nhighlight nothing, :last, an Int year, or a collection of years nothing Emphasise the given year(s) (full opacity, thicker line); every other year is drawn muted\n\nYear labelling: each line's own label is set to its year (R places the year at the line's right end rather than in a legend, which matters past ~12 years; whether a backend actually draws it that way is its own choice, not controlled here).\n\n\n\n\n\n","title":"SeasonalAdjustment.seasonalplot"},{"category":"function","location":"api/#SeasonalAdjustment.forecastplot","page":"API Reference","text":"forecastplot(r::X13Result; backcast=false, level=0.95, history=nothing)\n\nObserved series, then the forecast extension as a distinct series joined to the last observation ((dates[end], observed[end]) prepended, so the line doesn't start floating one period later), with the prediction interval drawn via a ribbon-attributed series covering ONLY the true forecast horizon (no join-point extension – there is no interval at a known observation).\n\nKeyword Values Default Meaning\nbackcast Bool false Also draw the backcast extension, joined the same way at dates[1]\nlevel Real in (0,1) 0.95 Interval width, passed to forecast/backcast\nhistory nothing, Int nothing Show only the last n observations before the forecast – truncates the OBSERVED series only, never the forecast/backcast extensions\n\n.fct/.bct already carry point/lower/upper on the original scale , so nothing here needs back-transforming.\n\n\n\n\n\n","title":"SeasonalAdjustment.forecastplot"},{"category":"function","location":"api/#SeasonalAdjustment.residdiagplot","page":"API Reference","text":"residdiagplot(r::X13Result; panels=[:series,:acf,:histogram], lags=24)\n\nThe standard X-13 residual review panel. Uses the binary's OWN .acf/.pcf/.ac2 (check{}-block tables) rather than recomputing from r.residuals, so the plotted values match what X-13 itself reports.\n\nKeyword Values Default Meaning\npanels any of :series, :acf, :pacf, :acfsquared, :histogram (:qq throws, see below) [:series, :acf, :histogram] Which panels, in order, one per row\nlags Int 24 Lags shown on the ACF/PACF/ACF² panels\n\n:qq deliberately throws rather than silently rendering nothing – it needs a normal quantile function this package doesn't have (the same open dependency question as elsewhere: is a Distributions.jl dependency worth adding for one panel? Not resolved here, so not silently guessed at either).\n\nConfidence bands on the ACF-family panels are the standard ±1.96/√n large-sample approximation (n = length(r.residuals)), drawn as two horizontal reference lines – .acf's own file DOES carry a proper per-lag standard error column (SE_of_ACF), not currently exposed here (only the primary statistic column is fetched, see SeasonalAdjustment._check_series).\n\n\n\n\n\n","title":"SeasonalAdjustment.residdiagplot"},{"category":"function","location":"api/#SeasonalAdjustment.componentplot","page":"API Reference","text":"componentplot(r::X13Result; which=:all, reference=true)\n\n** Closes the India-calendar loop**: coef(r) gives the Diwali coefficient itself; this shows its month-by-month path.\n\nKeyword Values Default Meaning\nwhich :all, :trading_day, :holiday, :user, :outlier :all Which factor series, via components\nreference Bool true Draw the no-effect reference line – read finmode from .udg rather than assumed: 1.0 for a multiplicative spec, 0.0 for additive\n\nComponents absent from the model are skipped silently (not drawn as flat lines) – which=:all with NO regression effects at all throws ArgumentError, via components's own check (not duplicated here).\n\n\n\n\n\n","title":"SeasonalAdjustment.componentplot"},{"category":"function","location":"api/#SeasonalAdjustment.spanplot","page":"API Reference","text":"spanplot(r::X13Result; kind=:slidingspans)\n\nIts scope genuinely depends on slidingspans/revision_history. Those two accessors deliberately surface .udg's HEADLINE summary statistics rather than the per-span TIME SERIES an earlier design sketch envisioned (slidingspans.sfspans etc. – separate saveable tables, not modeled here – a real, honest gap) – so this recipe's scope is correspondingly reduced: kind=:slidingspans draws the per-period average seasonal- factor revision (.udg's own s3.a.brk.pNN breakdown, the closest real per-period detail actually exposed); kind=:history draws the concurrent-vs-most-recent SEASONALLY ADJUSTED average-absolute-revision series revision_history already collects. NOT the full \"each span its own line\" chart originally sketched – that needs the raw per-span tables, a genuine future extension, not silently pretended here.\n\n\n\n\n\n","title":"SeasonalAdjustment.spanplot"},{"category":"function","location":"api/#SeasonalAdjustment.seasonalplot!","page":"API Reference","text":"seasonalplot(r::X13Result; series=:seasonal, polar=false, highlight=nothing)\n\n** The genuinely missing chart** – monthplot's transpose: calendar period on the x-axis, one line per YEAR, all overlaid, so an evolving seasonal pattern (the shape of the year drifting) is visible at a glance, the thing a subseries plot like monthplot can't show directly. Matches R's forecast::ggseasonplot/feasts::gg_season.\n\nKeyword Values Default Meaning\nseries :observed, :seasonal, :sa, :trend, :irregular :seasonal Which component to lay out\npolar Bool false Wrap onto a polar axis (gg_season's polar=TRUE) – backend support for this varies (confirmed Plots.jl-specific, not independently checked against every backend); decide per-backend rather than assuming\nhighlight nothing, :last, an Int year, or a collection of years nothing Emphasise the given year(s) (full opacity, thicker line); every other year is drawn muted\n\nYear labelling: each line's own label is set to its year (R places the year at the line's right end rather than in a legend, which matters past ~12 years; whether a backend actually draws it that way is its own choice, not controlled here).\n\n\n\n\n\nseasonalplot(r::X13Result; series=:seasonal, polar=false, highlight=nothing)\n\n** The genuinely missing chart** – monthplot's transpose: calendar period on the x-axis, one line per YEAR, all overlaid, so an evolving seasonal pattern (the shape of the year drifting) is visible at a glance, the thing a subseries plot like monthplot can't show directly. Matches R's forecast::ggseasonplot/feasts::gg_season.\n\nKeyword Values Default Meaning\nseries :observed, :seasonal, :sa, :trend, :irregular :seasonal Which component to lay out\npolar Bool false Wrap onto a polar axis (gg_season's polar=TRUE) – backend support for this varies (confirmed Plots.jl-specific, not independently checked against every backend); decide per-backend rather than assuming\nhighlight nothing, :last, an Int year, or a collection of years nothing Emphasise the given year(s) (full opacity, thicker line); every other year is drawn muted\n\nYear labelling: each line's own label is set to its year (R places the year at the line's right end rather than in a legend, which matters past ~12 years; whether a backend actually draws it that way is its own choice, not controlled here).\n\n\n\n\n\n","title":"SeasonalAdjustment.seasonalplot!"},{"category":"function","location":"api/#SeasonalAdjustment.forecastplot!","page":"API Reference","text":"forecastplot(r::X13Result; backcast=false, level=0.95, history=nothing)\n\nObserved series, then the forecast extension as a distinct series joined to the last observation ((dates[end], observed[end]) prepended, so the line doesn't start floating one period later), with the prediction interval drawn via a ribbon-attributed series covering ONLY the true forecast horizon (no join-point extension – there is no interval at a known observation).\n\nKeyword Values Default Meaning\nbackcast Bool false Also draw the backcast extension, joined the same way at dates[1]\nlevel Real in (0,1) 0.95 Interval width, passed to forecast/backcast\nhistory nothing, Int nothing Show only the last n observations before the forecast – truncates the OBSERVED series only, never the forecast/backcast extensions\n\n.fct/.bct already carry point/lower/upper on the original scale , so nothing here needs back-transforming.\n\n\n\n\n\nforecastplot(r::X13Result; backcast=false, level=0.95, history=nothing)\n\nObserved series, then the forecast extension as a distinct series joined to the last observation ((dates[end], observed[end]) prepended, so the line doesn't start floating one period later), with the prediction interval drawn via a ribbon-attributed series covering ONLY the true forecast horizon (no join-point extension – there is no interval at a known observation).\n\nKeyword Values Default Meaning\nbackcast Bool false Also draw the backcast extension, joined the same way at dates[1]\nlevel Real in (0,1) 0.95 Interval width, passed to forecast/backcast\nhistory nothing, Int nothing Show only the last n observations before the forecast – truncates the OBSERVED series only, never the forecast/backcast extensions\n\n.fct/.bct already carry point/lower/upper on the original scale , so nothing here needs back-transforming.\n\n\n\n\n\n","title":"SeasonalAdjustment.forecastplot!"},{"category":"function","location":"api/#SeasonalAdjustment.residdiagplot!","page":"API Reference","text":"residdiagplot(r::X13Result; panels=[:series,:acf,:histogram], lags=24)\n\nThe standard X-13 residual review panel. Uses the binary's OWN .acf/.pcf/.ac2 (check{}-block tables) rather than recomputing from r.residuals, so the plotted values match what X-13 itself reports.\n\nKeyword Values Default Meaning\npanels any of :series, :acf, :pacf, :acfsquared, :histogram (:qq throws, see below) [:series, :acf, :histogram] Which panels, in order, one per row\nlags Int 24 Lags shown on the ACF/PACF/ACF² panels\n\n:qq deliberately throws rather than silently rendering nothing – it needs a normal quantile function this package doesn't have (the same open dependency question as elsewhere: is a Distributions.jl dependency worth adding for one panel? Not resolved here, so not silently guessed at either).\n\nConfidence bands on the ACF-family panels are the standard ±1.96/√n large-sample approximation (n = length(r.residuals)), drawn as two horizontal reference lines – .acf's own file DOES carry a proper per-lag standard error column (SE_of_ACF), not currently exposed here (only the primary statistic column is fetched, see SeasonalAdjustment._check_series).\n\n\n\n\n\nresiddiagplot(r::X13Result; panels=[:series,:acf,:histogram], lags=24)\n\nThe standard X-13 residual review panel. Uses the binary's OWN .acf/.pcf/.ac2 (check{}-block tables) rather than recomputing from r.residuals, so the plotted values match what X-13 itself reports.\n\nKeyword Values Default Meaning\npanels any of :series, :acf, :pacf, :acfsquared, :histogram (:qq throws, see below) [:series, :acf, :histogram] Which panels, in order, one per row\nlags Int 24 Lags shown on the ACF/PACF/ACF² panels\n\n:qq deliberately throws rather than silently rendering nothing – it needs a normal quantile function this package doesn't have (the same open dependency question as elsewhere: is a Distributions.jl dependency worth adding for one panel? Not resolved here, so not silently guessed at either).\n\nConfidence bands on the ACF-family panels are the standard ±1.96/√n large-sample approximation (n = length(r.residuals)), drawn as two horizontal reference lines – .acf's own file DOES carry a proper per-lag standard error column (SE_of_ACF), not currently exposed here (only the primary statistic column is fetched, see SeasonalAdjustment._check_series).\n\n\n\n\n\n","title":"SeasonalAdjustment.residdiagplot!"},{"category":"function","location":"api/#SeasonalAdjustment.componentplot!","page":"API Reference","text":"componentplot(r::X13Result; which=:all, reference=true)\n\n** Closes the India-calendar loop**: coef(r) gives the Diwali coefficient itself; this shows its month-by-month path.\n\nKeyword Values Default Meaning\nwhich :all, :trading_day, :holiday, :user, :outlier :all Which factor series, via components\nreference Bool true Draw the no-effect reference line – read finmode from .udg rather than assumed: 1.0 for a multiplicative spec, 0.0 for additive\n\nComponents absent from the model are skipped silently (not drawn as flat lines) – which=:all with NO regression effects at all throws ArgumentError, via components's own check (not duplicated here).\n\n\n\n\n\ncomponentplot(r::X13Result; which=:all, reference=true)\n\n** Closes the India-calendar loop**: coef(r) gives the Diwali coefficient itself; this shows its month-by-month path.\n\nKeyword Values Default Meaning\nwhich :all, :trading_day, :holiday, :user, :outlier :all Which factor series, via components\nreference Bool true Draw the no-effect reference line – read finmode from .udg rather than assumed: 1.0 for a multiplicative spec, 0.0 for additive\n\nComponents absent from the model are skipped silently (not drawn as flat lines) – which=:all with NO regression effects at all throws ArgumentError, via components's own check (not duplicated here).\n\n\n\n\n\n","title":"SeasonalAdjustment.componentplot!"},{"category":"function","location":"api/#SeasonalAdjustment.spanplot!","page":"API Reference","text":"spanplot(r::X13Result; kind=:slidingspans)\n\nIts scope genuinely depends on slidingspans/revision_history. Those two accessors deliberately surface .udg's HEADLINE summary statistics rather than the per-span TIME SERIES an earlier design sketch envisioned (slidingspans.sfspans etc. – separate saveable tables, not modeled here – a real, honest gap) – so this recipe's scope is correspondingly reduced: kind=:slidingspans draws the per-period average seasonal- factor revision (.udg's own s3.a.brk.pNN breakdown, the closest real per-period detail actually exposed); kind=:history draws the concurrent-vs-most-recent SEASONALLY ADJUSTED average-absolute-revision series revision_history already collects. NOT the full \"each span its own line\" chart originally sketched – that needs the raw per-span tables, a genuine future extension, not silently pretended here.\n\n\n\n\n\nspanplot(r::X13Result; kind=:slidingspans)\n\nIts scope genuinely depends on slidingspans/revision_history. Those two accessors deliberately surface .udg's HEADLINE summary statistics rather than the per-span TIME SERIES an earlier design sketch envisioned (slidingspans.sfspans etc. – separate saveable tables, not modeled here – a real, honest gap) – so this recipe's scope is correspondingly reduced: kind=:slidingspans draws the per-period average seasonal- factor revision (.udg's own s3.a.brk.pNN breakdown, the closest real per-period detail actually exposed); kind=:history draws the concurrent-vs-most-recent SEASONALLY ADJUSTED average-absolute-revision series revision_history already collects. NOT the full \"each span its own line\" chart originally sketched – that needs the raw per-span tables, a genuine future extension, not silently pretended here.\n\n\n\n\n\n","title":"SeasonalAdjustment.spanplot!"},{"category":"function","location":"api/#SeasonalAdjustment.x13_binary_path","page":"API Reference","text":"x13_binary_path() -> String\n\nResolves the x13prebuilt artifact for the running platform and returns the actual invokable executable path within it – not just the artifact directory, since each platform's archive has a different internal layout (confirmed directly):\n\nLinux: a bare executable file at the artifact root (x13ashtml), installed via _linux_x13_artifact_dir since it isn't an archive Julia's normal installer can handle.\nWindows: x13ashtml/x13ashtml.exe (a subfolder), installed via _windows_x13_artifact_dir – a real zip archive, but one Julia's normal installer also can't handle (see that function's own docstring for the real bug this surfaced).\nmacOS: x13ashtml/bin/x13ashtml, dynamically linked against three .dylib files in the sibling x13ashtml/lib/ directory – this function asserts that directory and those specific files are present and throws a clear error if not, rather than let a broken extraction fail silently at first actual use. A real tar.gz, which Julia's normal installer genuinely does handle correctly (confirmed directly against a fresh artifact cache, unlike the Windows case above).\n\n\n\n\n\n","title":"SeasonalAdjustment.x13_binary_path"},{"category":"function","location":"api/#SeasonalAdjustment.x13_binary_available","page":"API Reference","text":"x13_binary_available() -> Bool\n\ntrue if the x13prebuilt binary can be resolved AND actually invoked (a trivial no-args run) on this platform, false otherwise (never throws). Exists so callers running or parsing a spec can give a clear, actionable error – \"the x13prebuilt binary could not be resolved for this platform\" – instead of a cryptic subprocess failure three layers deep.\n\n\n\n\n\n","title":"SeasonalAdjustment.x13_binary_available"},{"category":"type","location":"api/#SeasonalAdjustment.Calendar","page":"API Reference","text":"Calendar\n\nRoot abstract type for every calendar in this package – a QuantLib- style abstraction (isbusinessday/isholiday/isweekend/adjust/ advance/businessdaysbetween/holidaylist), deliberately its own hierarchy rather than a BusinessDays.HolidayCalendar subtype (see the comment above this type for why). TableCalendar is currently the only concrete subtype.\n\n\n\n\n\n","title":"SeasonalAdjustment.Calendar"},{"category":"type","location":"api/#SeasonalAdjustment.TableCalendar","page":"API Reference","text":"TableCalendar <: Calendar\n\nA calendar defined by (1) algorithmically-computable fixed holidays (functions year::Int -> Date), (2) a year-keyed table of moveable holidays with no closed-form date, and (3) a per-calendar set of weekend weekdays (Dates.dayofweek values, Monday=1..Sunday=7).\n\n\n\n\n\n","title":"SeasonalAdjustment.TableCalendar"},{"category":"constant","location":"api/#SeasonalAdjustment.INDIA_NSE","page":"API Reference","text":"INDIA_NSE::TableCalendar\n\nThe India NSE trading calendar. Weekend = Saturday/Sunday (Set([6,7]) in Dates.dayofweek terms). See NSE_MOVEABLE_HOLIDAYS's own docstring-adjacent comment for the honest gaps in the moveable-feast table.\n\n\n\n\n\n","title":"SeasonalAdjustment.INDIA_NSE"},{"category":"function","location":"api/#SeasonalAdjustment.isbusinessday","page":"API Reference","text":"isbusinessday(cal, d) -> Bool\n\ntrue unless d is a weekend (per cal's own weekend set) or a holiday (per cal's own fixed + table holidays).\n\n\n\n\n\n","title":"SeasonalAdjustment.isbusinessday"},{"category":"function","location":"api/#BusinessDays.isholiday","page":"API Reference","text":"isholiday(cal::TableCalendar, d::Date) -> Bool\n\ntrue if d matches one of cal's fixed-date holiday rules or its year-keyed moveable-holiday table. Extends BusinessDays.isholiday.\n\n\n\n\n\n","title":"BusinessDays.isholiday"},{"category":"function","location":"api/#BusinessDays.isweekend","page":"API Reference","text":"isweekend(cal::Calendar, wd::Integer) -> Bool\n\ntrue if weekday wd (a Dates.dayofweek value, Monday=1..Sunday=7) is in cal's own weekend set. Extends BusinessDays.isweekend, whose own single-argument isweekend(dt::Date) method hardcodes Saturday/ Sunday for every calendar – this per-calendar method is exactly the capability Calendar's own docstring explains BusinessDays.jl can't express.\n\n\n\n\n\n","title":"BusinessDays.isweekend"},{"category":"function","location":"api/#SeasonalAdjustment.adjust","page":"API Reference","text":"adjust(cal, d, convention=:following) -> Date\n\nStandard business-day conventions (:following, :preceding, :modified_following, :modified_preceding, :unadjusted), applied to d under cal. Returns d unchanged if it's already a business day (or if convention == :unadjusted).\n\n\n\n\n\n","title":"SeasonalAdjustment.adjust"},{"category":"function","location":"api/#SeasonalAdjustment.advance","page":"API Reference","text":"advance(cal, d, period, convention=:following) -> Date\n\nShifts d by the calendar period (e.g. Dates.Month(1)), then applies adjust with convention to the result.\n\n\n\n\n\n","title":"SeasonalAdjustment.advance"},{"category":"function","location":"api/#SeasonalAdjustment.businessdaysbetween","page":"API Reference","text":"businessdaysbetween(cal, from, to) -> Int\n\nCount of business days in the closed interval [min(from,to), max(from,to)].\n\n\n\n\n\n","title":"SeasonalAdjustment.businessdaysbetween"},{"category":"function","location":"api/#SeasonalAdjustment.holidaylist","page":"API Reference","text":"holidaylist(cal, from, to; include_weekends=false) -> Vector{Date}\n\nHoliday dates in [min(from,to), max(from,to)]. include_weekends additionally includes every weekend date in range (per cal's own weekend set), not just named holidays.\n\nErrors loudly (ArgumentError) if any year spanned by the range has no table_holidays entry in cal – silently falling back to fixed holidays only would look complete while quietly missing most of a real moveable-feast calendar.\n\n\n\n\n\n","title":"SeasonalAdjustment.holidaylist"},{"category":"function","location":"api/#SeasonalAdjustment.easter_date","page":"API Reference","text":"easter_date(year) -> Date\n\nEaster Sunday for year, computed by BusinessDays.jl's own easter_date/easter_rata (not re-derived here).\n\n\n\n\n\n","title":"SeasonalAdjustment.easter_date"},{"category":"function","location":"api/#SeasonalAdjustment.trading_day_regressors","page":"API Reference","text":"trading_day_regressors(from, to, cal; freq=:month) -> Matrix{Float64}\n\nFor each period between from and to (freq=:month or freq=:quarter, matching X-13's own period=12/period=4), count actual business days (per cal) on each weekday, returned as a (nperiods, 6) matrix using X-13's own usertype=td contrast convention: column j (Monday=1..Saturday=6) is (# business days on weekday j in that period) - (# business days on Sunday in that period).\n\nKnown gap, freq=:quarter: confirmed directly against the real binary that quarterly's own .rmx export for td has a 7th column (\"Leap Year\") that monthly's does not – this function always returns 6 columns for either freq, so it does not reproduce that extra column. Fine for feeding a user-defined regressor (X-13 doesn't require the extra column there), not a byte-for-byte replica of X-13's own internal quarterly td regressor.\n\n\n\n\n\n","title":"SeasonalAdjustment.trading_day_regressors"},{"category":"function","location":"api/#SeasonalAdjustment.easter_regressor","page":"API Reference","text":"easter_regressor(from, to; window=0, freq=:month) -> Vector{Float64}\n\nThe standard Census/X-13 Easter regressor: for each period (freq=:month or freq=:quarter) between from and to, the fraction of the window-day window immediately before Easter Sunday (of whichever year that period falls in) that overlaps the period. window=0 (the default) produces an all-zero vector – X-13 itself has no universal default w, the user always specifies it explicitly in the .spc file, so no particular value is assumed here either; pass an explicit window (commonly 1, 8, or 15 in practice). freq=:quarter confirmed directly against the real binary to work the same way as monthly (Easter always falls within Q1 or Q2, so at most one quarter per year is affected).\n\n\n\n\n\n","title":"SeasonalAdjustment.easter_regressor"},{"category":"function","location":"api/#SeasonalAdjustment.custom_holiday_regressor","page":"API Reference","text":"custom_holiday_regressor(from, to, cal, holiday_years_present; freq=:month) -> Vector{Float64}\n\nFor each period (freq=:month or freq=:quarter) between from and to, 1.0 if holiday_years_present(year) (a function year::Int -> Union{Date,Nothing}) returns a date that both falls in that period AND is not already a weekend under cal (a holiday that lands on a weekend has no incremental trading-day effect to explain – matches the \"no extra closure\" annotation real NSE holiday listings use for exactly this case), 0.0 otherwise. A year for which holiday_years_present returns nothing contributes 0.0, silently – by design, this function treats \"not applicable this year\" and \"not in the table\" the same way; callers who need to distinguish a genuine gap from a holiday that simply didn't occur should audit coverage with holidaylist first, which errors loudly on an untabulated year.\n\nThis is the exact mechanism verified end-to-end against the real x13prebuilt binary (via regression { user = (...) usertype = (holiday) }, a synthetic Diwali-effect test that genuinely shifted the fitted seasonal factors) – this function produces the data vector that block needs; the spec-rendering layer is responsible for writing the block itself.\n\n\n\n\n\n","title":"SeasonalAdjustment.custom_holiday_regressor"},{"category":"function","location":"api/#SeasonalAdjustment.dataset","page":"API Reference","text":"dataset(name) -> (date=Vector{Date}, value=Vector{Float64})\ndataset(name, sink)\n\nname is \"airline\"/:airline/etc – see datasets for the full list. Returns a plain NamedTuple (a Tables.jl column table with no Tables.jl dependency of its own) by default; a deepcopy of the package's own cached copy, so mutating what you get back can't corrupt a later call in the same session (the two-arg sink form skips this – a sink materialises its own storage, e.g. dataset(\"airline\", DataFrame)).\n\nNo dataset(names::AbstractVector) method, deliberately. Broadcasting is the vector story (dataset.([\"airline\", \"appliance\"])) – if both existed, dataset(names) and dataset.(names) would be different operations spelled almost identically.\n\nSink failures pass through unchanged. dataset(\"airline\", Vector) fails with MethodError: no method matching Vector(::NamedTuple...), which names the real problem – no custom fallback error wraps it.\n\nConfirmed sinks: DataFrame, TSFrame (its own docs confirm CSV.read(path, TSFrame), i.e. any Tables.jl source), Tables.rowtable, Tables.matrix (mixed Date/Float64 gives Matrix{Any}). TimeArray as a BARE sink is unverified – use x -> TimeArray(x; timestamp=:date) if the bare form needs a hint.\n\nresult = x13(dataset(\"airline\"))   # start is inferred from tsindex, see below\n\n\n\n\n\n","title":"SeasonalAdjustment.dataset"},{"category":"function","location":"api/#SeasonalAdjustment.datasets","page":"API Reference","text":"datasets() -> Vector{String}\ndatasets(sink)\n\nEvery bundled dataset's name, sorted (\"airline\", \"appliance\", \"appliance_q\", \"iip_india\") – or, with a sink, a Tables.jl table of every dataset's own metadata (name/title/frequency/n/span/units/ licence/kind), one row per dataset.\n\n\n\n\n\n","title":"SeasonalAdjustment.datasets"},{"category":"function","location":"api/#SeasonalAdjustment.dataset_info","page":"API Reference","text":"dataset_info(name) -> DatasetInfo\n\nFull provenance for one bundled dataset – source, licence, citation, span, and (for iip_india) an explicit note on what was and wasn't independently re-verified this session. Answers \"may I publish a chart of this\" without re-deriving it from notes prose each time.\n\n\n\n\n\n","title":"SeasonalAdjustment.dataset_info"},{"category":"type","location":"api/#SeasonalAdjustment.DatasetInfo","page":"API Reference","text":"DatasetInfo\n\nProvenance for one bundled dataset (dataset_info(name)) – name/ title/source/url/licence/citation/frequency/n/span/ units/retrieved/notes/kind. kind is one of :published (a real, sourced series), :derived (built from another shipped dataset – appliance_q from appliance), or :synthetic (none shipped yet; reserved for still-outstanding teaching examples) – added now rather than deferred, since this session already ships one :derived dataset that needs to say so structurally, not just in prose.\n\n\n\n\n\n","title":"SeasonalAdjustment.DatasetInfo"},{"category":"section","location":"getting_started/#Getting-Started","page":"Getting Started","text":"note: About the examples on this page\nEvery example below was actually run, not just written and assumed correct — the pure-Julia ones (calendars, spec construction, bundled datasets) are live jldoctest blocks Documenter re-verifies on every docs build; the ones that call the real binary were confirmed directly against it during development but are shown as plain code blocks rather than live doctests, since running the actual x13prebuilt binary isn't something every environment that might build these docs can do. Every concrete number quoted on this page was independently re-verified against a real run this session, not copied from a draft without checking.","title":"Getting Started"},{"category":"section","location":"getting_started/#1.-Installation-and-first-check","page":"Getting Started","text":"using Pkg\nPkg.add(\"SeasonalAdjustment\")\n\nThat is the whole installation. There is no second step.\n\nThis is worth pausing on: the X-13ARIMA-SEATS binary itself does not need a separate download, a PATH entry, or a location passed to every call. SeasonalAdjustment.jl ships the binary as a Julia artifact, resolved for your platform when it's first needed. Linux, macOS and Windows are all covered. Nothing is placed on your PATH, nothing is installed system-wide, and different Julia environments can hold different versions without interfering.","title":"1. Installation and first check"},{"category":"section","location":"getting_started/#Checking-it-works","page":"Getting Started","text":"using SeasonalAdjustment\n\nx13_binary_available()\n\ntrue\n\nx13_binary_available resolves the binary for your platform and then actually invokes it. It returns false rather than throwing, so it's safe to use as a guard in scripts and test suites.\n\nIf you want the path itself, x13_binary_path resolves it (the exact value is platform-dependent, so not reproduced here) — the path differs by platform in a way that's worth knowing about only if something goes wrong. On Linux the executable sits at the artifact root. On Windows it's inside an x13ashtml/ subdirectory. On macOS it's at x13ashtml/bin/x13ashtml and is dynamically linked against three libraries in a sibling lib/ directory, which x13_binary_path checks for and reports clearly if they're missing.","title":"Checking it works"},{"category":"section","location":"getting_started/#If-the-check-fails","page":"Getting Started","text":"x13_binary_available() returning false means one of two things.\n\nThe artifact didn't resolve for your platform. This happens on platforms outside the prebuilt set, most often unusual Linux architectures. There is no workaround inside the package; you'd need to build X-13 from the Census Bureau's Fortran source and point the package at it through run_x13's binary_path keyword.\n\nOr the artifact resolved but the binary wouldn't run. On macOS this is usually the missing-library case above, and the error message names the specific file. Clearing the artifact cache and reinstalling fixes most instances.","title":"If the check fails"},{"category":"section","location":"getting_started/#What-you-have-just-installed","page":"Getting Started","text":"The binary is x13ashtml, built from the U.S. Census Bureau's own source. It's the program national statistical offices run in production.\n\nThis matters for a reason that will come up repeatedly. SeasonalAdjustment.jl does not reimplement seasonal adjustment. It builds a specification file, runs the Census binary, and parses what comes back into Julia types. When you compare results against a statistical office's published figures, you're comparing against the same computation, not an independent one that happens to agree.","title":"What you have just installed"},{"category":"section","location":"getting_started/#2.-Your-first-adjustment","page":"Getting Started","text":"","title":"2. Your first adjustment"},{"category":"section","location":"getting_started/#The-series","page":"Getting Started","text":"The package ships a few datasets, so nothing in this guide depends on data you have to find first.\n\njulia> using SeasonalAdjustment\n\njulia> datasets()\n4-element Vector{String}:\n \"airline\"\n \"appliance\"\n \"appliance_q\"\n \"iip_india\"\n\nWe want the first one.\n\njulia> using SeasonalAdjustment\n\njulia> d = dataset(\"airline\");\n\njulia> length(d.value), first(d.date), last(d.date)\n(144, Dates.Date(\"1949-01-01\"), Dates.Date(\"1960-12-01\"))\n\nMonthly totals of international airline passengers, January 1949 to December 1960, in thousands. Box and Jenkins used it in Time Series Analysis: Forecasting and Control and it's been the standard example ever since. It appears throughout the seasonal adjustment literature and in this package's own verification corpus. If you've read anything at all about seasonal adjustment, you've seen this series.\n\nWhere it came from, and whether you may republish a chart of it:\n\ndataset_info(\"airline\")\n\nInternational airline passengers (\"airline\")\n  Source:     Box & Jenkins (1976), Series G\n  Licence:    Public domain\n  Kind:       published\n  Frequency:  12 (monthly)\n  N:          144\n  Span:       1949-01-01 .. 1960-12-01\n  Units:      thousands of passengers\n  Citation:   Box, G.E.P. and Jenkins, G.M. (1976). Time Series\n              Analysis: Forecasting and Control. Holden-Day. Series G.\n\nEvery dataset carries its source, licence and citation. That's deliberate: a figure in a paper needs an attribution line, and guessing at one is worse than looking it up. iip_india is the one exception worth knowing about up front — its own dataset_info says plainly that its exact redistribution terms weren't independently re-verified when it was bundled, so check dataset_info(\"iip_india\") before republishing anything built from it.\n\ndataset returns a NamedTuple of date and value, which happens to be a valid Tables.jl table. So if you'd rather have something else:\n\nusing DataFrames\ndataset(\"airline\", DataFrame)\n\nAny Tables.jl sink works: DataFrame, TSFrame, Tables.rowtable, Tables.matrix, or anything else that accepts a table.\n\nThe airline series is a good first example because everything it does, it does clearly.\n\n(Image: Airline passengers, 1949-1960)\n\nFigure 2.1. Two things are visible immediately. The series trends upward, roughly doubling and then doubling again. And within each year there's a repeating shape, peaking in summer, with a smaller bump in December. The yearly shape is not constant in size: the summer peak in 1960 is far larger in absolute terms than the one in 1949.\n\nThat last point turns out to matter, and section 4 returns to it.","title":"The series"},{"category":"section","location":"getting_started/#Adjusting-it","page":"Getting Started","text":"res = x13(dataset(\"airline\"))\n\nOne call, and no start argument. The dataset carries its dates, and x13 reads them to work out that the series begins in January\n\n\n\nFor your own data you'd say so explicitly:\n\nres = x13(y; start = (1949, 1))                # y a plain Vector, monthly\nres = x13(y; start = (1990, 1), period = 4)    # quarterly\n\nThe result is an X13Result. Four series come back, each a field:\n\nField X-11 table What it is\nseasonally_adjusted D11 the series with the seasonal pattern removed\ntrend D12 the smooth underlying trend-cycle\nseasonal_factors D10 the estimated seasonal pattern itself\nirregular D13 what's left over\n\nPlus residuals from the regARIMA model, and udg, the binary's own diagnostics dump, which section 3 uses heavily.\n\nThe X-11 table names in that middle column will keep appearing. They are how the X-13 documentation and every statistical office refer to these series, so the package keeps them visible rather than hiding them behind Julia names alone.","title":"Adjusting it"},{"category":"section","location":"getting_started/#Looking-at-it","page":"Getting Started","text":"plot(res)\n\n(Image: Original and seasonally adjusted)\n\nFigure 2.2. The original series and the adjusted one, overlaid. The summer peaks and winter troughs are gone. What remains still rises, and still wobbles, but the wobbles are no longer the calendar.\n\nThis is the chart to look at first, every time. If the adjusted line doesn't look like a sensible version of the original with the yearly rhythm taken out, nothing further is worth checking.\n\nFor the components separately:\n\nplot(res; panels = :components)\n\n(Image: Four-panel decomposition)\n\nFigure 2.3. Observed, seasonally adjusted, trend, and irregular. The trend panel is the adjusted series smoothed further. The irregular panel is what neither the trend nor the seasonal pattern explains, and on a well-behaved series it should look like noise around 1.0.","title":"Looking at it"},{"category":"section","location":"getting_started/#What-actually-happened-—-and-what-this-bare-call-did-*not*-do","page":"Getting Started","text":"It's tempting to assume the single call above did everything X-13 can do automatically: tested the transform, searched for an ARIMA model, checked for outliers. It didn't — and checking that honestly is a better first lesson than assuming it.\n\ntransformfunction(res), arima_model(res)\n\n(:none, \"(0 0 0)\")\n\nNo transform was tested (transformfunction returns :none, not because X-13 tried logs and rejected them, but because nothing was asked for), and no real ARIMA model was fit — (0 0 0) is X-13's own trivial fallback when neither an explicit model nor automdl is requested. The adjustment in Figures 2.2–2.3 still ran (X-11's own filtering doesn't need a fitted regARIMA model to decompose a series), but this bare call is the \"nothing turned on\" baseline, not the \"X-13 doing its usual automatic work\" case.\n\nfilters(res)\n\n(seasonal_ma = [\"MSR\", \"MSR\", \"MSR\", \"MSR\", \"MSR\", \"MSR\", \"MSR\", \"MSR\", \"MSR\", \"MSR\", \"MSR\", \"MSR\"],\n trend_ma = 9, mode = nothing, sa_mode = \"multiplicative seasonal adjustment\", unit_root = nothing)\n\nfilters reports the seasonal moving average X-11 chose for each calendar month (its own default, MSR, here), the trend filter length, and the decomposition mode — X-11 still defaults to a multiplicative decomposition even though no transform was tested at the regARIMA stage; the two are separate choices. X-11 selects these from properties of the data rather than applying fixed defaults, and section 4 shows how to override them.\n\nAnd the whole specification at once:\n\nstatic(res)\n\nstatic resolves every automatic decision into an explicit specification — here, since nothing was automatic to begin with, it mostly just echoes the bare defaults back (arima_model=\"(0 0 0)\", transform left at :none, no outliers to list). It becomes genuinely useful once automatic selection is actually turned on, which is where section 3 picks up.","title":"What actually happened — and what this bare call did not do"},{"category":"section","location":"getting_started/#Before-moving-on","page":"Getting Started","text":"You have an adjusted series and you have not yet checked a single thing about whether it's any good — and, as the section above shows, you haven't yet asked X-13 to do its usual automatic work either. That's the subject of the next section, and it's not optional. Seasonal adjustment fails quietly. A bad adjustment produces a smooth, plausible-looking line that happens to be wrong, and nothing about Figure 2.2 would tell you.","title":"Before moving on"},{"category":"section","location":"getting_started/#3.-Was-it-any-good?","page":"Getting Started","text":"Seasonal adjustment fails quietly. There's no error, no warning, no obviously wrong number. You get a smooth line that looks like an adjusted series, and it may be wrong in ways that matter.\n\nSo there's a battery of diagnostics, and it's unusually large compared with most statistical procedures. This section runs the five checks worth making every time, in the order worth making them — against a fuller specification than section 2's bare call, one that actually lets X-13 make its usual automatic choices:\n\nres = x13(dataset(\"airline\");\n          automdl = true,\n          outlier = true,\n          aictest = [:td, :easter],\n          transform = :auto)\n\nThis is the specification behind every verified number in this section and the next.","title":"3. Was it any good?"},{"category":"section","location":"getting_started/#Check-1:-does-the-seasonal-pattern-look-sensible?","page":"Getting Started","text":"monthplot(res)\n\n(Image: Seasonal factors by month with SI ratios)\n\nFigure 3.1. Twelve bands, one per calendar month. The thick horizontal bar in each band is the estimated seasonal factor. The vertical stems are the SI ratios, which are what the data actually showed in each individual year before smoothing.\n\nRead it this way. The bars tell you the shape of the average year: July and August well above 1.0, February well below. The stems tell you how much year-to-year variation the bar is smoothing through. Tight stems mean a stable seasonal pattern that's easy to estimate. Widely scattered stems mean the pattern is moving, and the single bar is a compromise.\n\nThe stems drifting steadily in one direction within a band is the signal to watch for. It means that month's seasonal effect is changing over time, and a single factor is hiding a trend.","title":"Check 1: does the seasonal pattern look sensible?"},{"category":"section","location":"getting_started/#Check-2:-is-there-seasonality-left?","page":"Getting Started","text":"The point of the exercise is to remove the seasonal pattern. The QS test asks whether any remains.\n\nqs(res)\n\nSeries QS statistic p-value\noriginal 167.65 0.000\nseasonally adjusted 0.00 1.000\n\nThat's the pattern to want. Strong, highly significant seasonality in the input. None detectable in the output.\n\nThe failure mode is a significant QS on the adjusted series. It means the procedure didn't get everything, and the adjusted figures still contain a calendar rhythm. On monthly economic data that's a publication-blocking result at most statistical offices.","title":"Check 2: is there seasonality left?"},{"category":"section","location":"getting_started/#Check-3:-the-M-statistics","page":"Getting Started","text":"X-11 produces eleven summary statistics, M1 to M11, and combines them into an overall Q. Each M targets a different way an adjustment can go wrong: too much irregular relative to trend, seasonal factors moving too fast, and so on.\n\nThe convention is simple. Below 1.0 passes. Above 1.0 fails.\n\nm = mstats(res)\nm.q, m.m7, m.fail\n\n(0.20, 0.203, 0)\n\nQ of 0.20 against a threshold of 1.0 is a comfortable pass. fail is the count of individual M statistics that exceeded 1.0, and zero is what you want.\n\nM7 deserves separate attention. It tests whether identifiable seasonality is present at all, and it's the one most practitioners quote. An M7 above 1.0 is often taken to mean the series shouldn't be seasonally adjusted, because there's not enough stable seasonality there to remove. At 0.203, this series has plenty.\n\nmstats returns all eleven plus q, qm2 and fail. It returns nothing for a SEATS run, since the M statistics are specific to X-11.","title":"Check 3: the M statistics"},{"category":"section","location":"getting_started/#Check-4:-spectral-peaks","page":"Getting Started","text":"A seasonal pattern in monthly data shows up as peaks at particular frequencies. If those peaks are still in the adjusted series, seasonality survived.\n\nspectrumplot(res; series = :sa)\n\n(Image: Spectrum of the adjusted series)\n\nFigure 3.2. The spectrum of the adjusted series, with vertical markers at any seasonal or trading-day frequency X-13 flagged as a visually significant peak. A clean adjustment has no markers at the seasonal frequencies.\n\nFor a summary rather than a picture:\n\nspectral_peaks(res)\n\nspectral_peaks tells you which series show peaks. spectrum_peaks (singular spectrum, note) tells you at which frequency, which is the finer information the plot uses.\n\nA trading-day peak is a different message from a seasonal one. It says the series responds to the day-of-week composition of the month, and section 4 shows how to model it.","title":"Check 4: spectral peaks"},{"category":"section","location":"getting_started/#Check-5:-is-the-model-adequate?","page":"Getting Started","text":"The regARIMA model at the front of the pipeline has its own diagnostics, and they're ordinary time series diagnostics.\n\nresidplot(res)\n\n(Image: regARIMA residuals)\n\nFigure 3.3. Residuals against time, with a zero reference line. You are looking for something that resembles noise: no drift, no fanning out, no runs of same-signed values.\n\nd = residual_diagnostics(res)\nd.durbin_watson, d.skewness, d.kurtosis\n\n(1.9504, 0.0900, 3.0698)\n\nDurbin-Watson near 2.0 indicates no first-order residual autocorrelation. Skewness near 0 and kurtosis near 3 are what a normal distribution gives, so these residuals are close to normal.\n\nd.ljung_box is the full lag-indexed table rather than a single number, because the underlying diagnostic is computed at several lags and the answer can differ between them.","title":"Check 5: is the model adequate?"},{"category":"section","location":"getting_started/#The-checklist","page":"Getting Started","text":"Check Function Want\nSensible seasonal pattern monthplot tight stems, no drift within a band\nSeasonality removed qs significant on original, not on adjusted\nOverall quality mstats Q below 1.0, fail of 0\nNo residual seasonality spectral_peaks no seasonal peak in :sa\nModel adequate residual_diagnostics DW near 2, no Ljung-Box rejection\n\nTwo more exist and are worth knowing about even though they're beyond this guide. seasonality_tests gives the stable and moving seasonality F-tests and the identifiable-seasonality verdict. slidingspans/revision_history ask a different question again: not whether the adjustment is right today, but whether it will still look like this next month (see section 5).","title":"The checklist"},{"category":"section","location":"getting_started/#When-a-check-fails","page":"Getting Started","text":"Do not reach for a different method. Reach for the specification.\n\nA failed QS on the adjusted series, or a seasonal spectral peak, usually means the model in front of the filter is missing something. An outlier, a calendar effect, a level shift. Those are section 4.","title":"When a check fails"},{"category":"section","location":"getting_started/#4.-When-the-defaults-aren't-enough","page":"Getting Started","text":"Three settings account for most of what you'll ever change. This section takes them one at a time and then puts them together.","title":"4. When the defaults aren't enough"},{"category":"section","location":"getting_started/#Multiplicative-or-additive","page":"Getting Started","text":"Look again at Figure 2.1. The summer peak in 1960 is much larger in absolute terms than the one in 1949. But as a proportion of that year's level, the two are similar. July is roughly 20% above the year's average in both.\n\nThat distinction is the whole question. When the seasonal swing grows with the level of the series, the pattern is multiplicative and the model belongs in logs. When the swing stays a constant number of units regardless of level, it's additive.\n\nX-13 tests this for you when asked (transform = :auto, as in section 3's res):\n\ntransformfunction(res)\n\n:log\n\nLog, meaning multiplicative. To see why it matters, force the other choice:\n\nd = dataset(\"airline\")\nres_log  = x13(d; transform = :log)\nres_none = x13(d; transform = :none)\n\n(Image: Multiplicative versus additive adjustment)\n\nFigure 4.1. The two adjusted series, and their difference. Early in the sample they're close. Late in the sample, where the seasonal swing is largest, they separate. The additive version removes a constant-sized summer effect from years where the actual effect was much larger, and leaves visible seasonality behind.\n\nLeave transform at its default unless you have a reason. The reasons that do come up: a series containing zero or negative values cannot be logged, and X-13 will pick :none for you; and if you're comparing across a series break or reproducing someone else's published figures, you may need to pin the choice rather than let it be re-tested each period.","title":"Multiplicative or additive"},{"category":"section","location":"getting_started/#Outliers","page":"Getting Started","text":"A strike, a policy change, a pandemic. A single unusual observation distorts the estimated seasonal pattern for that calendar month in every year, because the seasonal factor for July is estimated from all the Julys.\n\nDetection is off by default:\n\nres = x13(dataset(\"airline\"); outlier = true)\noutliers(res)\n\nOn the airline series with the fuller specification above, one is found:\n\nlabel type year period\nAO1951.May :ao 1951 5\n\nAn additive outlier in May 1951, one month, no lasting effect.\n\nWorth noticing: the values around it are 163, 172, 178. Nothing about that looks unusual. Detection works on regARIMA residuals after differencing, not on levels, so it finds things the eye does not.\n\nplot(res; outliers = true)\noutlier_counts(res)\n\n(Image: Detected outlier marked)\n\nFigure 4.2. The adjustment with the detected outlier marked.\n\nThree types are detected automatically. An additive outlier (AO) is a single odd observation. A level shift (LS) is a permanent step to a new level. A temporary change (TC) is a jump that decays back. outlier_counts gives how many of each were found.\n\nOne caution that applies everywhere and is easy to forget. Automatic detection near the end of a series is unreliable, because there isn't yet enough data after the event to distinguish a temporary blip from a permanent shift. A level shift detected in the most recent few months should be treated as provisional.","title":"Outliers"},{"category":"section","location":"getting_started/#Calendar-effects","page":"Getting Started","text":"Months contain different numbers of Mondays. A series driven by business activity will be higher in a month with five Mondays than one with four, and this has nothing to do with the season.\n\nres = x13(dataset(\"airline\"); trading = true, transform = :log)\n\nOr let X-13 decide whether the effect is there at all:\n\nres = x13(dataset(\"airline\"); aictest = [:td, :easter], transform = :auto)\n\naictest fits the regressors, compares information criteria with and without, and keeps them only if they earn their place. On the airline series both are kept, and the trading-day F-test is emphatic:\n\nF(1, 128) = 31.06,  p = 1.4e-7\n\nEaster is the other built-in worth knowing. It moves between March and April, so its effect splits across two months in a way that changes every year, and a fixed seasonal factor cannot absorb it. Easter[1] in the output means a one-day window before Easter Sunday.\n\nX-13 has built-in regressors for Easter, Labor Day and Thanksgiving. All three are US holidays. For Diwali, Chinese New Year, Eid, or anything else, this package provides custom_holiday_regressor and a calendar layer; section 5 points the way.","title":"Calendar effects"},{"category":"section","location":"getting_started/#Choosing-the-ARIMA-model","page":"Getting Started","text":"X-13 can search for the model rather than being told one:\n\nres = x13(dataset(\"airline\"); automdl = true, transform = :auto)\narima_model(res)\n\n\"(0 1 1)(0 1 1)\"\n\nThis is worth a remark. (0 1 1)(0 1 1) is the model Box and Jenkins fitted to this series by hand in 1970. It's so closely associated with it that the specification is called the airline model, and it remains the default starting point for monthly economic series across the profession. X-13's automatic procedure arrives at it independently.\n\nTo see what it considered and rejected:\n\nfivebestmdl(res)\n\n5-element Vector{@NamedTuple{model::String, bic::Float64}}:\n (model = \"(0 1 0)(0 1 1)\", bic = -4.007)\n (model = \"(1 1 1)(0 1 1)\", bic = -3.986)\n (model = \"(0 1 1)(0 1 1)\", bic = -3.979)\n (model = \"(1 1 0)(0 1 1)\", bic = -3.977)\n (model = \"(0 1 2)(0 1 1)\", bic = -3.97)\n\nfivebestmdl returns the five best candidates with their BIC values, in rank order. Useful when the chosen model surprises you (notice (0 1 1)(0 1 1) isn't even the top BIC here — automdl weighs more than raw BIC when picking among close candidates), and useful for judging how decisive the choice was: five candidates within a hair of each other means the selection is close to arbitrary.\n\nIf all you want is the order, without the adjustment:\n\nselect_order(dataset(\"airline\"))\n\n(order = (3, 1, 1), seasonal_order = (0, 1, 1, 12), transform = :none)\n\nNote this is a different model from the one above — select_order runs automdl on its own, with no aictest/transform requested, so it searches under different conditions than section 3's fuller res.","title":"Choosing the ARIMA model"},{"category":"section","location":"getting_started/#Putting-it-together","page":"Getting Started","text":"res = x13(dataset(\"airline\");\n          automdl = true,\n          outlier = true,\n          aictest = [:td, :easter],\n          transform = :auto)\n\nThis is the specification behind every verified number in this guide (the same one section 3 introduced).\n\nThe diagnostics, all independently verified against this exact call:\n\nDiagnostic Value Verdict\nTransform Log(y) multiplicative\nARIMA model (0 1 1)(0 1 1) the airline model\nQ 0.20 pass\nM7 0.203 identifiable seasonality\nFailed M statistics 0 pass\nQS, original 167.65, p = 0.000 seasonality present\nQS, adjusted 0.00, p = 1.000 seasonality removed\nDurbin-Watson 1.95 no residual autocorrelation\nOutliers found 1 (AO, May 1951) ","title":"Putting it together"},{"category":"section","location":"getting_started/#Freezing-the-result","page":"Getting Started","text":"Automatic selection is convenient during exploration and a liability in production. Next month's data may change the chosen model, and your published figures will move for reasons that have nothing to do with the economy.\n\nfrozen = static(res)\n\nstatic returns a specification with every automatic choice resolved: the ARIMA order fixed, detected outliers listed explicitly, the transform pinned. Run that specification from then on and the model stops moving underneath you.\n\nOne caveat the docstring covers and is worth repeating. Re-running a frozen specification reproduces the original to about six significant figures, not bit-identically, because estimation converges slightly differently when a model is given rather than searched for. Compare with isapprox, not ==.","title":"Freezing the result"},{"category":"section","location":"getting_started/#Everything-else","page":"Getting Started","text":"These settings cover most cases. The rest of what X-13 can do runs to 280 pages of Census Bureau documentation.\n\nSome of it is reachable through named keywords, and the API reference lists them. Anything without a named keyword goes through spec_args, which passes raw \"block.argument\" pairs straight to the specification file:\n\nres = x13(dataset(\"airline\");\n          spec_args = Dict(\"forecast.maxlead\" => \"12\"))\n\nThat escape hatch means the package never blocks you from something X-13 can do.","title":"Everything else"},{"category":"section","location":"getting_started/#5.-Where-to-go-next","page":"Getting Started","text":"","title":"5. Where to go next"},{"category":"section","location":"getting_started/#What-you-can-do-now","page":"Getting Started","text":"Adjust a monthly or quarterly series, look at the result, run the five checks that matter, and change the settings that come up most. That covers a large share of routine work.","title":"What you can do now"},{"category":"section","location":"getting_started/#The-other-document","page":"Getting Started","text":"API Reference is the authority on signatures, keywords, defaults and known gaps. Every function named in this guide has an entry there with more detail than this guide gives, deliberately. When you want to know what a keyword accepts, go there and not here.","title":"The other document"},{"category":"section","location":"getting_started/#Things-this-guide-did-not-cover","page":"Getting Started","text":"The other datasets. appliance is the Census Bureau's own worked example from the X-13 manual, a US retail series with a strong December peak and a real trading-day effect. Its seasonal shape is completely unlike the airline series, which makes it a good second thing to try.\n\nres = x13(dataset(\"appliance\"); aictest = [:td, :easter], transform = :auto)\n\nQuarterly data. Everything here works with period = 4. Dates, tick labels and diagnostics all follow.\n\nres = x13(dataset(\"appliance_q\"); period = 4, seasonal_order = (0, 1, 1, 4))\n\nSEATS. X-13 contains a second, quite different adjustment engine, based on decomposing a fitted ARIMA model rather than filtering. x13(d; seats = true) switches to it. The M statistics don't apply; the other diagnostics do.\n\nCalendar effects outside the US. X-13's built-in moving holidays are Easter, Labor Day and Thanksgiving. This package adds a calendar layer and custom_holiday_regressor for anything else. The India NSE calendar ships as INDIA_NSE, Diwali is the worked example (it moves between October and November, so no fixed seasonal factor can absorb it), and dataset(\"iip_india\") — India's real monthly Index of Industrial Production, with a genuine COVID level shift — is a real series to try it against:\n\njulia> using SeasonalAdjustment, Dates\n\njulia> isholiday(INDIA_NSE, Date(2025, 10, 21))  # Diwali-Laxmi Pujan 2025\ntrue\n\njulia> custom_holiday_regressor(Date(2025, 9, 1), Date(2025, 12, 31), INDIA_NSE,\n                                 year -> year == 2025 ? Date(2025, 10, 21) : nothing)\n4-element Vector{Float64}:\n 0.0\n 1.0\n 0.0\n 0.0\n\nForecasts and missing values. The regARIMA model produces forecasts and prediction intervals, not just the extension X-11 uses internally, and x13 can interpolate a missing value rather than requiring a clean series:\n\nf = forecast(res; level = 0.95)   # (dates=, point=, lower=, upper=)\nx13(y; missing_action = :x13)     # interpolates via a regARIMA estimate\n\nComponent-factor time paths and model diagnostics beyond the five checks. components gives a regression effect's own month-by-month factor (the Diwali coefficient's actual shape, not just its estimate); StatsAPI.vcov/StatsBase.coeftable give the regression/ARIMA coefficient covariance matrix and a full coefficient table; slidingspans/revision_history ask whether an adjustment will still look the same next month. Five more plot recipes — seasonalplot, forecastplot, residdiagplot, componentplot, spanplot — cover these visually; see the API reference.\n\nforce/seasonalma. Forcing seasonally adjusted annual totals to match the original series, and pinning a specific seasonal moving-average filter instead of X-11's own choice:\n\nx13(y; force = :denton)\nx13(y; seasonalma = :s3x9)\n\nMany series at once. generate_specs and run_x13_batch run a panel in parallel. Datasets broadcast, so a quick comparison is one line:\n\nresults = x13.(dataset.([\"airline\", \"appliance\"]))\n\nOther output tables. The four component series are four of 281 tables X-13 can write. series fetches any of them, re-running automatically if the table wasn't saved the first time.\n\nThe full HTML report. open_output opens the binary's own output in your browser, which is where the exhaustive detail lives.\n\nMigration. import_spc reads an existing .spc file, so specs from other X-13 tooling come across without hand-translation.","title":"Things this guide did not cover"},{"category":"section","location":"getting_started/#Reading-beyond-the-documentation","page":"Getting Started","text":"The X-13ARIMA-SEATS Reference Manual from the Census Bureau is the authoritative source on every specification and every option. Chapter 7 is the per-spec reference and Appendix B lists the output tables.\n\n**Ladiray and Quenneville, *Seasonal Adjustment with the X-11 Method*** (2001) is the book on X-11 itself, table by table. It predates X-12, so it covers neither regARIMA nor SEATS, but nothing else explains the filtering at that resolution.\n\n**Dagum and Bianconcini, *Seasonal Adjustment Methods and Real Time Trend-Cycle Estimation*** (2016) is the modern treatment and the standard reference for SEATS.\n\nFindley, Monsell, Bell, Otto and Chen (1998), in the Journal of Business and Economic Statistics, is the design paper for everything X-12 added over X-11, including the sliding spans and revision history diagnostics.\n\nEurostat's ESS Guidelines on Seasonal Adjustment is free and is about practice rather than algorithm: when to force annual totals, how often to re-identify models, what revision policy to adopt.","title":"Reading beyond the documentation"},{"category":"section","location":"getting_started/#A-closing-note","page":"Getting Started","text":"Two habits are worth carrying forward.\n\nRun the checks. An adjustment that fails a diagnostic still produces a smooth, plausible line, and nothing about looking at it will tell you.\n\nFreeze the specification before publishing. static exists so that your figures move when the economy moves, and not when the model selection does.","title":"A closing note"},{"category":"section","location":"getting_started/#Design-notes-worth-knowing-before-you-dig-further","page":"Getting Started","text":"The one deliberate exception in the TSAnalytics.jl family. This package wraps the real x13prebuilt binary rather than reimplementing X-11/RegARIMA/SEATS from scratch — see Design principles on the Home page for why.\nX13Spec/run_x13/parse_output are the lower-level API behind x13 — reach for them directly if you need a custom, partial table selection (x13() itself always fetches the full D10-D13/ S10-S13 quartet) or want to inspect the rendered .spc text before running it.\nPlatform support: Linux, Windows, and macOS are all resolved via x13_binary_path/x13_binary_available, each platform's own archive layout (a bare file, a zip subfolder, and a bin/+lib/ directory pair, respectively) handled automatically.","title":"Design notes worth knowing before you dig further"},{"category":"section","location":"#SeasonalAdjustment.jl","page":"Home","text":"Official-statistics-grade seasonal adjustment for Julia — X-11, RegARIMA, and SEATS — built on the same trusted, freely-redistributable Census Bureau binary (x13prebuilt) national statistical offices run in production, with India- and other-market-aware calendar effects (Diwali, Holi, and beyond) fed into RegARIMA via x13prebuilt's own user-defined-regressor mechanism — something not available out of the box elsewhere.\n\nPart of the TSAnalytics.jl project.\n\nStatus: the x13prebuilt wrapper (X-11, RegARIMA, SEATS, diagnostics, plotting, bundled datasets) is complete. A from-scratch native Julia engine hasn't started yet.\n\nNew to the package? Start with Getting Started.\nLooking for a specific function? See the API Reference.","title":"SeasonalAdjustment.jl"},{"category":"section","location":"#Design-principles","page":"Home","text":"Wrap the real binary, don't reimplement it — the one deliberate exception to the TSAnalytics.jl family's usual \"reference, never port\" rule. Rather than reimplementing X-11/RegARIMA/SEATS from scratch, this package wraps x13prebuilt directly — the exact binary national statistical offices already trust for the hardest part of the whole problem (SEATS's spectral factorization especially); wrapping it directly gets correct, production-grade output immediately rather than spending months re-deriving the hardest mathematics in the field before shipping anything. A from-scratch native Julia engine remains a real, planned track, deliberately sequenced behind a working wrapper, not abandoned.\nA genuine superset of R's seas() and Python's x13_arima_analysis(), not just a naming exercise. R exposes the entire X-13 spec grammar dynamically via ...; Python exposes only a curated, typed subset. x13's own keyword arguments combine both: R-style raw passthrough (regression_variables, arima_model) for anything the curated fields don't cover, layered with Python-style curated ergonomics (maxorder, maxdiff, trading, outlier, seasonal_order, ... — matching statsmodels.tsa.x13.x13_arima_analysis's own real parameter names) for discoverability.\nValidated before a subprocess round-trip is ever spent. validate! checks every real requirement this project confirmed directly against the actual binary (minimum series length, regressor forecast-horizon coverage, the transform=:log/multiplicative-mode interaction, the ARIMA-vs-automdl conflict) natively, in microseconds, before X13Spec is ever rendered and run — neither R's nor Python's wrapper does this validation at all.\nTyped results, not raw files or bare text. run_x13 returns a real X13RunResult (success, warnings, errors already extracted from the binary's own stdout — confirmed directly that the process exit code alone can't tell success from failure), and x13 returns a typed X13Result, not a directory of output tables the caller has to go find and parse themselves.\nVerified against the real binary directly, not a third-party reimplementation. There is no independent second computation to cross-check against here — matching correctly means matching x13prebuilt's own real output, so every committed verification fixture is real, generated output from actually running the pinned binary, reused as a regression fixture rather than hand-written.","title":"Design principles"}]
}
